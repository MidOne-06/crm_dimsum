# CRM DIMSUM (opm-digemid)

> Este archivo es un espejo de `CLAUDE.md` en la raíz del repo -- mismo
> contenido, para que Claude Code y Codex/ChatGPT partan del mismo
> contexto. Si editás uno, actualizá el otro igual (especialmente la
> Bitácora al final).

> Actualización 2026-09-08 (Codex): auditoría del canje masivo: un reintento
> de cola ya no duplica la evidencia ni el contador local de un movimiento;
> cancelar una previsualización exige el permiso específico. La bitácora de
> este ciclo queda reflejada también en la entrega de auditoría correspondiente.

Panel Filament 5.7 sobre Laravel 11, PHP-FPM, Postgres. Repo remoto:
`https://github.com/MidOne-06/crm_dimsum.git` (rama `main`). Producción:
`2.25.155.29` (migrado desde `2.25.104.73` el 2026-09-04 -- ver Bitácora;
el servidor viejo quedó apagado, sin borrar, como respaldo frío), Docker
Compose, proyecto `crm-dimsum`, deploy en `/opt/crm-dimsum`. Puerto
`8080` (comparte el VPS con otro proyecto ajeno, `dimsum-inventory`, que
usa 8081/443 -- no tocar esos contenedores). Trabaja junto al gateway
`API-TI` (`D:\DS-TI\API-TI`, repo `api-ti`, deploy en `/opt/API-TI`), que
expone la integración real contra Restaurant.pe vía Playwright.

Este proyecto se edita con más de una herramienta de IA (Claude Code y
Codex/ChatGPT, al menos). Las reglas de abajo existen porque ya hubo
fricción real por trabajar sin ellas: una herramienta dejó cambios sin
commitear mientras la otra trabajaba en el mismo árbol, y solo se
descubrió por accidente (unos logs `codex-guias-*.log` sueltos en
`storage/`). No fue un problema de Git -- fue falta de disciplina antes
de empezar a tocar el árbol.

## Reglas de convivencia entre herramientas

**Antes de empezar a editar cualquier archivo:**
1. `git status --short` -- el árbol debe estar limpio. Si hay cambios
   sin commitear que no son tuyos de esta sesión, **no los toques, no
   los descartes** (nunca `git stash`/`git checkout --` sobre trabajo
   ajeno) -- repórtalo al usuario y esperá instrucción.
2. `git pull` -- traé lo que la otra herramienta ya haya subido.

**Al terminar una sesión de trabajo (o antes de que el usuario abra la
otra herramienta):**
3. Commitear y hacer `git push` de lo que quedó terminado y probado.
   No dejar el árbol sucio "para después" -- la próxima sesión (con
   cualquiera de las dos herramientas) parte de ahí.
4. Agregar una entrada corta en la Bitácora de abajo: qué se hizo, qué
   quedó pendiente, y cualquier advertencia que la próxima sesión
   necesite saber (ej. "no reintentar X sin ver Y primero").

**Si vas a trabajar en paralelo de verdad** (no por turnos, sino con
las dos herramientas activas al mismo tiempo): usar una rama por
herramienta (`claude/<tema>`, `codex/<tema>`) y mergear a `main`
cuando cada una termine, en vez de compartir el mismo checkout local.
Evita por completo el problema de "árbol sucio de otro proceso".

**Producción solo se despliega por versión, nunca copiando el disco
local.** Ver el skill `deploy-produccion` (`.claude/skills/deploy-produccion/`)
para el flujo completo y por qué existe.

## Bitácora

Formato: `AAAA-MM-DD · herramienta · qué se hizo · qué queda pendiente/advertencias`.
Agregar entradas nuevas al final. No editar entradas viejas salvo para
corregir un error real.

- 2026-09-03 · Claude Code · Directiva de Transferencia Fase 0 (9 módulos de configuración: cadencia por local, vida útil, capacidad de fábrica, prorrateo, sustitución, capacidad de vehículo, cantidad de arranque) + trazabilidad (`configuracion_dt_eventos`) en todos los modelos de configuración. · Los 9 módulos existen pero están vacíos -- falta cargar datos operativos reales antes de poder construir Fase 1 (cantidad sugerida).
- 2026-09-03 · Claude Code · Guías internas: extracción migrada al estándar de modales; scripts `deploy-production.ps1`/`verify-production.ps1` corregidos y verificados contra producción real (5 bugs reales encontrados y cerrados, no solo revisados en código). · Ninguno.
- 2026-09-03 · Claude Code · Nuevo módulo Reporte de guías internas (`guias-internas/reporte`), con el mismo patrón que Reporte de requerimientos; "Aplicar filtros" sincroniza contra Restaurant antes de mostrar/exportar. · Ninguno.
- 2026-09-03 · Claude Code · Corrige bug real de integridad: `guias-internas:sincronizar` reconciliaba (borraba) siempre por `fecha_emision` sin importar que el usuario hubiera elegido "Fecha de traslado" -- podía borrar guías válidas. Ahora `--filtro-fecha` viaja hasta la reconciliación. · Ninguno.
- 2026-09-03 · Claude Code · Extracción de guías internas: la pantalla solo rastreaba "la corrida más reciente por id", así que una corrida más vieja atascada quedaba invisible bloqueando el botón sin explicación. Ahora se listan TODAS las corridas activas, con detección de estancamiento (15 min sin avance) y cancelación por id. `init: true` agregado al servicio `scheduler` en `compose.yaml` (corría como PID 1 sin reaparentar procesos hijos, acumulando zombies). · Ninguno.
- 2026-09-03 · Claude Code · Intento de optimizar `guias-internas:sincronizar` pidiendo el detalle de cada página en paralelo (`Http::pool`, aprovechando el pool de 4 sesiones del gateway). **Revertido** (commit `d1f73d4`): colgaba el proceso de forma reproducible justo después de la página 1, en 3 pruebas con métodos de lanzamiento distintos, incluido el mecanismo real de producción (despachador + `BackgroundArtisan`). · **No reintentar sin antes conseguir visibilidad real de errores** -- `BackgroundArtisan` manda todo a `/dev/null`, así que la causa exacta del cuelgue nunca se vio. Antes de reintentar: correr con salida capturada (no `/dev/null`) en un entorno controlado, y sospechar primero de una interacción entre `Http::pool()` y el cliente HTTP compartido de Laravel (posible conexión/curl-multi que queda en mal estado para la siguiente llamada no-pooled).
- 2026-09-03 · Codex · Reconciliación de producción: seis páginas de Configuración DT que existían solo en el artefacto del VPS fueron incorporadas al repositorio para evitar drift. · Todo despliegue debe usar `scripts/deploy-production.ps1`; no copiar archivos al VPS.
- 2026-09-03 · Codex · Extracción de guías: corrige la zona horaria de la sesión PostgreSQL, normaliza los timestamps históricos de sus corridas y conserva el stderr de Artisan en `storage/logs/background-artisan.log`. · Verificar la corrida #64 y revisar ese log ante cualquier futura interrupción.
- 2026-09-03 · Codex · Listado de guías internas migrado a consulta paginada en tiempo real contra Restaurant; el reporte permanece sobre la copia local modelada. · Detalle, anulación y descargas del listado también consultan Restaurant; extracción continúa alimentando informe e histórico.
- 2026-09-03 · Claude Code · Migración de guías/salidas/requerimientos de `BackgroundArtisan` (moría en cada deploy que recreara `scheduler`) a jobs reales en el contenedor `worker` (`SincronizarGuiasInternasJob`, `SincronizarSalidasStockJob`, `SincronizarRequerimientosStockJob`), con `retryUntil()` en vez de `$tries` fijo (64 fallos falsos por `MaxAttemptsExceededException` corregidos) y guarda de estado para no reprocesar corridas ya `completado` (encontrado en 4/5 despachos duplicados reales de stock-actual/salidas). Corrige además la reanudación de Stock Actual, que relanzaba en `--directo` y quedaba en loop sin avanzar nunca. `reanudar-huerfanas` ahora cubre los 6 módulos de sincronización. · Ninguno -- verificado en producción con `verify-production.ps1` y con una corrida real interrumpida y reanudada.
- 2026-09-03 · Claude Code · Corrige el gap arquitectónico de fondo detrás de las 3 interrupciones reales de la corrida #71 este mismo día: `GuiasInternasHistoricoService::sincronizar()` siempre reiniciaba desde la página 1 al reanudar una corrida `en_progreso`, perdiendo todo el progreso ante cualquier interrupción (deploy, restart). Ahora detecta reanudación (`estado='en_progreso' && paginas_procesadas>0`) y continúa desde `paginas_procesadas+1`, reconstruyendo `$seen` desde lo ya persistido en BD; si una corrida previa a la reanudación dejó páginas sin leer o errores, la reconciliación (borrado) se sigue omitiendo para no generar falsos positivos. Verificado en producción: la corrida #71 reanudó en la página 11 (no desde 0) tras el redeploy. También se encontraron y purgaron 12 despachos duplicados de esa misma corrida acumulados por reintentos previos (payloads con `retryUntil` nulo, de antes del fix), y un lock huérfano con prefijo de caché `panel_administrativo_*` (de un `APP_NAME` anterior, sin dueño activo). `VENTAS_WORKER_REPLICAS` 3→5 y `RESTAURANT_SESSION_POOL_SIZE` 8→12 (VPS con ~12GB RAM libre y load average <1 de 4 núcleos, sin OOM-kills registrados). · `RESTAURANT_SESSION_POOL_SIZE=12` quedó escrito en `/opt/API-TI/.env` pero el contenedor `gateway` no se reinició todavía (habría cortado sesiones de extracciones activas); toma efecto recién en el próximo reinicio natural de ese contenedor.
- 2026-09-03 · Claude Code · Bug de timestamp encontrado en producción: una conexión Postgres de larga duración en `worker` escribía `updated_at` con ~5h de atraso (Lima vs UTC), haciendo que el detector de "extracción huérfana" de la pantalla de Guías internas mostrara "sin avance hace 5 horas" sobre corridas activas y sanas (#71, #70) -- riesgo real de que alguien cancelara una extracción viva pensando que estaba muerta. Causa raíz exacta no confirmada (se sospecha de la conexión persistente del `worker` de mayor antigüedad); mitigado reiniciando las conexiones (`--force-recreate` de `worker`), confirmado con timestamps correctos después. · Sin causa raíz de código identificada y corregida -- si vuelve a aparecer "sin avance" con datos que sí están progresando, sospechar primero de este mismo bug antes de asumir que el proceso murió, y considerar agregar una verificación defensiva (`SET timezone` explícito en cada conexión, o un healthcheck que compare `now()` vs hora del sistema).
- 2026-09-03 · Claude Code · Reanudación incremental por página extendida a `SalidasStockHistoricoService` (mismo patrón que Guías internas, ver entrada anterior). Verificado en producción con una corrida real (#106): estaba en progreso en la página 3/6, se recreó `worker` a propósito para probar, y al redespachar retomó exactamente en la página 4 (paginas_procesadas 3→6 sin reiniciar), cerrando en `completado`. · Requerimientos (`SincronizarReporteRequerimientos`) sigue sin esta misma reanudación incremental -- no se ha demostrado el problema ahí todavía (corre cada 30 min sin cortes hoy), pero el mismo patrón aplicaría si algún día se corta a mitad de una corrida grande. Auditoría de Kardex: el pipeline (`ExtraerKardexJob`/`ProcesarLocalKardexJob`) ya tenía buen diseño de reintento/reclamo (sin el bug de `MaxAttemptsExceededException` falso, porque no usa `release()` manual) -- sin cambios de código. Hallazgo real pendiente de decisión: Kardex **no tiene extracción automática programada** (a diferencia de Ventas/Guías/Requerimientos que corren cada 30 min); depende 100% de que alguien la dispare a mano -- la última corrida real fue del 2026-09-01. Evaluar con el usuario si se agrega al scheduler.
- 2026-09-03 · Claude Code · Cobertura de requerimientos: matriz achicada a pedido del usuario (quedaba muy alta/ancha) -- celdas h-2.5, padding casi cero, texto 10px, nombre de local truncado con tooltip. Misma estructura y datos, solo más compacta. Desplegado y verificado. · Ninguno.
- 2026-09-03 · Claude Code · Cobertura de requerimientos: nueva matriz "todos los locales a la vez" (pedido explícito del usuario, eligió esta opción entre 3 propuestas). Antes había que elegir un local del selector uno por uno para ver su calendario -- ahora, arriba del calendario anual existente, una tabla local (fila) x día del mes (columna) coloreada igual (verde/ámbar/vacío) con % de cobertura por fila; clic en el nombre de un local salta al calendario detallado de abajo. `coverageMatrix()` hace una sola pasada por las corridas del mes (no una consulta por local) reutilizando el mismo criterio de coincidencia id/nombre que `coverageMap()`. Verificado en producción con un usuario de diagnóstico: colores correctos (verde para los días con datos reales, gris tenue para días futuros del mes), clic en local actualiza el selector de detalle, navegación de mes presente. · Ninguno -- misma vista podría replicarse en Guías internas/Salidas si el usuario lo pide más adelante (ahí la cobertura hoy también es por-local, un local a la vez).
- 2026-09-03 · Claude Code · Pedido explícito del usuario: encolar varias extracciones sin bloquear el botón. `DespacharSincronizacionesPendientes` ya tomaba una corrida a la vez (la más vieja primero) -- el bloqueo del botón era solo de la UI, quitarlo era seguro. `iniciarExtraccion()` (Guías internas y Requerimientos) ya no rechaza si hay otra en curso; el panel lista TODAS las activas/en cola con su posición, y nuevo `eliminarDeCola($id)` para sacar una 'pendiente' antes de que arranque. Verificado en vivo con 2 corridas reales encoladas seguidas (#57 en progreso, #58 "1° en la cola") -- funcionó tal cual se pidió. · De paso se encontró un SEGUNDO bug de "nombre de prop equivocado" en las mismas dos páginas (mismo patrón que `form`/`form-id` de la entrada anterior): `x-slot name="headerEnd"` no es un prop real de `x-filament::section` -- el correcto es `afterHeader`. Un slot no reconocido no da error, simplemente no se renderiza: los botones "Detener" (y el nuevo "Quitar de la cola") nunca aparecían en la pantalla, en NINGÚN momento -- **"Detener" en Guías internas estuvo roto toda la sesión sin que nadie lo notara**, porque nunca se probó desde la UI real hasta esta verificación. Corregido y verificado en producción llamando a los métodos del componente directamente (`eliminarDeCola`/`cancelarExtraccion` confirmados funcionando). Lección para toda la sesión: **cuando se usan slots con nombre en componentes `x-filament::*`, verificar el nombre real contra `vendor/filament/.../*.blade.php` (`@props`) en vez de asumirlo por convención** -- ya van dos bugs reales (`form`/`form-id`, `headerEnd`/`afterHeader`) causados exactamente por esto, y ambos fallan en silencio sin ningún error visible.
- 2026-09-03 · Claude Code · Corrige la CAUSA REAL de "Iniciar extracción" inerte (el arreglo anterior de la misma sesión atacaba un problema real pero secundario). Replicando el flujo en vivo con un usuario de diagnóstico temporal y DevTools: en `<x-filament::button>`, el prop que escribe el atributo HTML `form="..."` es `form-id`, NO `form` (ese otro solo alimenta el indicador de carga interno -- `vendor/filament/support/src/View/Concerns/CanGenerateButtonHtml.php`). Con `form="..."` el botón "Iniciar extracción" (en el slot `footerActions`, fuera del `<form>` real) se renderizaba sin ningún atributo `form` -- confirmado en el DOM real (`formAttr: null`, `closestForm: null`). Un `<button type="submit">` sin form asociado no dispara nada al hacer clic, SIEMPRE, no solo cuando hay otra extracción en curso. Verificado extremo a extremo en producción: con el fix, un clic real creó la fila y despachó el job (extracción de prueba #56, cancelada después de confirmar). · Ninguno -- pero vale la pena revisar si el mismo error (`form=` en vez de `form-id=`) existe en algún botón de otro proyecto que use Filament 5.7+.
- 2026-09-03 · Claude Code · Corrige bug real reportado por el usuario: en Extracción de requerimientos (y el mismo patrón en Guías internas), abrir el modal "Iniciar extracción" mientras otra corrida está activa (la sincronización automática de cada 30 min, u otro usuario) dejaba el botón deshabilitado en silencio -- un submit deshabilitado nunca dispara `wire:submit`, así que `iniciarExtraccion()` ni corre y su propio mensaje de error nunca se muestra. Se veía exactamente como "no pasa nada, se queda colgado". Confirmado con datos reales de producción: la sincronización automática de requerimientos corre cada 30 min (45-70s normalmente, una vez 70 minutos completos), ventana real donde un usuario puede caer. Arreglo: aviso visible dentro del modal + `wire:poll` de 5s mientras esté bloqueado, para que el botón se reactive solo. Desplegado recreando solo `app` (sin tocar `worker`/`scheduler`, para no interrumpir la extracción manual real -- #55 -- que estaba en curso en ese momento; terminó bien, 215/215). Ventas y Kardex no tenían este bug -- ya comunican el estado cambiando el propio texto del botón. · Ninguno.
- 2026-09-03 · Claude Code · Defensa contra el bug de timezone stale: `Queue::before()` en `AppServiceProvider` reafirma `set time zone` antes de CADA job de cola, en vez de depender solo del `SET TIME ZONE` que `PostgresConnector` aplica al abrir la conexión (que un worker reutiliza hasta 1h, `--max-time=3600`, a través de docenas de jobs). Cierra el hueco sin haber confirmado la causa raíz exacta de por qué la conexión de la corrida #71 quedó atrasada. Verificado en producción: corrida real de requerimientos (#54) con `updated_at` a 8 segundos de `now()`, no 5 horas. · Ninguno.
- 2026-09-03 · Claude Code · Cierra los 2 hallazgos de proceso de la auditoría de módulos: (1) nuevo comando `kardex:sincronizar-diario`, programado a las 03:15 -- extrae el día anterior para todos los locales, protegido contra solapes (`hayExtraccionEnProgreso`) e idempotente por fecha (`whereJsonContains` sobre `filtros`); (2) `ImportarPlantilla` (Requerimientos) ya no hace `->poll('60s')` en la tabla -- cada refresco automático repetía una consulta paginada completa a Restaurant.pe por cada local asignado del usuario, carga de fondo innecesaria compitiendo por el mismo pool de sesiones. También se extendió la reanudación incremental (mismo patrón de Guías/Salidas) a `SincronizarReporteRequerimientos::sincronizar()`: el job y el comando CLI ahora aceptan `en_progreso` además de `pendiente`, y `ReanudarExtraccionesHuerfanas::reanudarRequerimientos()` ya no necesita resetear el estado a `pendiente` antes de relanzar (eso descartaba el progreso guardado, igual que el bug original de guías). Verificado en producción con dos corridas reales simultáneas tras un redeploy intencional: Guías #75 reanudó en la página 12 (no desde 0) y Requerimientos #53 corrió limpio hasta `completado` (116/116). · Con esto, los 4 módulos de sincronización histórica (Guías, Salidas, Requerimientos, y Kardex por diseño propio) tienen la misma protección contra pérdida de progreso ante una interrupción.
- 2026-09-03 · Claude Code · Cobertura de requerimientos: se probó una matriz visual "todos los locales x día del mes" (a pedido del usuario), se compactó una vez, y finalmente se sacó por completo -- el usuario prefirió el calendario anual por local de siempre + un resumen de texto arriba ("N de M locales al día" + lista de solo los que tienen pendientes). `coverageMatrix()` quedó como método protegido (fuente de datos interna) consumido por el nuevo `coverageSummary()`.
- 2026-09-03 · Claude Code · Extracción de guías internas #80 reportada por el usuario "sin avance hace 5 horas" -- esta vez SÍ era un stall real (no el bug de timezone anterior: se comparó contra el reloj de Postgres y el atraso era genuino). Un job había quedado "RUNNING" para siempre sosteniendo el lock, sin nunca llegar a "DONE" pese a que el cliente HTTP tiene timeouts configurados (90-180s) -- causa raíz exacta no confirmada. Recuperado con el mismo procedimiento ya usado hoy para #71: purga de jobs duplicados en cola, `forceRelease()` del lock, redespacho. Verificado en producción: retomó en la página 4 (no desde 0) y siguió avanzando con normalidad. De paso se agregó `init: true` al servicio `gateway` en `compose.yaml` (no lo tenía, a diferencia de `scheduler` que ya se había corregido por el mismo motivo -- zombies de Playwright sin cosechar) como fix preventivo, aunque no se pudo confirmar que sea la causa raíz exacta de estos stalls. **No desplegado todavía** -- recrear `gateway` corta cualquier sesión de Restaurant.pe activa; esperar una ventana sin extracciones en curso. · Pendiente: desplegar el `init: true` de gateway en el próximo despliegue seguro, y seguir atento a si estos stalls de horas se repiten (sería la tercera vez del día -- #71 dos veces, #80 una).
- 2026-09-03 · Claude Code · Corrige bug real de cálculo en `coverageSummary()`, reportado por el usuario al cambiar a Agosto: los 37 locales salían "con pendientes" pese a que la corrida #59 había cubierto agosto completo sin errores para todos. Causa: `$monthEnd` se calculaba con `endOfMonth()` (23:59:59) mientras `$monthStart` usaba `startOfDay()` (00:00:00) -- esa asimetría hacía que `diffInDays()` devolviera un float (`30.999999999988`) en vez de 30, y un mes 100% cubierto (31/31 días) terminaba calculando 97%, no 100% -- como el filtro es `pct < 100`, TODOS los locales aparecían con pendientes aunque no tuvieran ninguno. Fix: `startOfDay()` en ambos extremos + cast a int. Verificado en producción: agosto y septiembre ahora muestran 0 locales con pendientes, como corresponde. · Ninguno -- pero deja una lección: cualquier `diffInDays()`/cálculo de rango de fechas en este código debe alinear explícitamente la hora de ambos extremos (`startOfDay()` en los dos, no solo uno) para evitar este mismo tipo de error de redondeo silencioso.
- 2026-09-03 · Claude Code · Segundo stall real del día en Guías internas (#80, misma clase que #71): un job quedó "RUNNING" sosteniendo el lock sin nunca completar. Recuperado igual que antes (purga de jobs + `forceRelease()` + redespacho) -- retomó en la página 17 y cerró en `completado` (888 cabeceras, 10.124 detalles, 0 errores). Se aprovechó el reinicio forzoso para desplegar el `init: true` de `gateway` (pendiente desde la entrada anterior) -- verificado que el PID 1 ahora es `/sbin/docker-init` en vez de `node server.js` directo. Después del redeploy la extracción completó normal, pero **no hay forma de confirmar si el `init: true` fue la causa real o coincidencia** -- solo un stall observado después del fix, y terminó bien.
- 2026-09-03 · Claude Code · Mapea a Guías internas el resumen de cobertura por mes que se había agregado en Requerimientos (pedido explícito del usuario) -- mismo patrón exacto: `coverageMonth`, `coveragePrevMonth()`/`coverageNextMonth()`, `coverageMatrix()` protegido, `coverageSummary()` con el fix de `startOfDay()` en ambos extremos ya incluido desde el vamos (no hubo que redescubrir el bug, se copió ya corregido). La cola de extracciones múltiples ya estaba mapeada desde antes en este mismo módulo -- esto cierra la paridad completa entre ambas pantallas de Extracción. Verificado en producción por reflexión (sin login de navegador, que seguía fallando por el mismo motivo de sesiones de diagnóstico): agosto y septiembre devuelven 0 locales con pendientes para guías internas también, sin excepciones.
- 2026-09-03 · Claude Code · **CAUSA RAÍZ REAL encontrada** de los stalls de horas vistos hoy (#71 x2, #80, #82 en guías internas; #116 en salidas de stock): `SincronizarGuiasInternasJob`/`SincronizarSalidasStockJob`/`SincronizarRequerimientosStockJob` declaran `$timeout=7200` en la clase, pero ese valor **nunca se aplicaba de verdad** -- el `--timeout=720` de la CLI de `queue:work` en `compose.yaml` es el que Laravel hace cumplir en la práctica (vía SIGKILL a un proceso hijo), y gana el menor de los dos. Como estos jobs procesan TODAS las páginas de una corrida dentro de un solo `handle()`, una sola página lenta (muchos detalles, sesión de Restaurant.pe contendida) superaba los 12 minutos y el worker mataba el proceso a la fuerza. Un SIGKILL no ejecuta el `finally` que libera el `Cache::lock`, así que quedaba tomado hasta por 4h (su TTL) sin que nada real lo estuviera usando -- eso, no el `init: true` de `gateway` de la entrada anterior (que quedó como fix preventivo pero no era la causa real). Subido a `--timeout=3300` (55 min). Verificado en producción: #82 y #116 (ambas encontradas stalleadas al mismo tiempo, mismo patrón, confirmando el diagnóstico) retomaron sin perder progreso y siguieron avanzando con normalidad tras el redeploy. · Con esto, esta clase de stall no debería repetirse -- si vuelve a pasar pese al nuevo timeout, sería evidencia de un hang genuino (no solo una página lenta) y ameritaría revisar los timeouts HTTP internos del gateway.
- 2026-09-04 · Claude Code · Nuevo Panel de Sincronización (`/admin/sincronizacion`) pedido por el usuario: activar/desactivar por módulo el ciclo automático de los 6 syncs (`configuracion_sincronizaciones` + `ConfiguracionSincronizacion::activo()`, consumido por `->when()` en cada `Schedule::command()` de `routes/console.php`), con historial de últimas 20 corridas por módulo. Nuevo comando `ventas:sincronizar-diario` (Ventas era el único módulo de los 6 sin sync incremental automático) y backfill completo de Requerimientos 2025 (era un hueco real, no cubierto -- verificado 88=88, 1189=1189 contra BD real). · Ninguno.
- 2026-09-04 · Claude Code · Nuevo módulo Kardex → Consolidado de ventas (`/admin/kardex/consolidado-ventas`): cantidad vendida (unidades, no monto) por producto y local, catálogo fijo de 23 productos en el orden exacto pedido por el usuario (fila en cero si no hubo venta, no desaparece), modo "comparar días específicos" con columnas local×fecha o suma de fechas + "ocultar locales sin venta", export a Excel/PDF. Fuente: `kardex_movimientos` con `motivo='SALIDA, POR VENTA.'` en Almacén Principal (excluye `'SALIDA, POR VENTA (ANULADA - MERMA).'`). Bug real encontrado y corregido: el `DatePicker` del Repeater de fechas a veces guardaba la fecha con la hora actual pegada, lo que hacía fallar en silencio el cruce interno de filas y dejaba esa fecha en cero en toda la tabla (el filtro SQL igual funcionaba bien, el bug estaba en el cruce PHP) -- fix con `->format('Y-m-d')` en el DatePicker + normalización defensiva en el servidor. Filtros de todas las pantallas de Kardex (Promedios de venta, Histórico, Análisis de descargas, Extracción) migrados al estándar de modales. · Ninguno.
- 2026-09-04 · Claude Code · **Migración completa de servidor de producción**: `2.25.104.73` → `2.25.155.29` (pedido explícito del usuario, VPS nuevo comparte máquina con otro proyecto ajeno `dimsum-inventory` en puertos 8081/443 -- CRM Dimsum quedó en su puerto de siempre, 8080, sin conflicto). Migrado: código (clon fresco del mismo commit), base de datos completa (28 GB, dump/restore -Fc, verificado con conteo exacto de fila en las 13 tablas más grandes -- ventas 827.794=827.794, kardex_movimientos 2.595.921=2.595.921, sin ninguna diferencia), y contenido real no versionado (storage, catálogo de productos, `.env.docker`, `.env` del gateway). Corte hecho con el sistema viejo pausado (`docker compose stop` de scheduler+workers, no solo el toggle del Panel de Sincronización) para garantizar cero escrituras durante el dump; servidor viejo detenido al final (sin borrar, respaldo frío). Verificado con `verify-production.ps1 -HostName 2.25.155.29`: 395 archivos coinciden byte a byte. · **Dos hallazgos reales durante la migración, para tener en cuenta en la próxima**: (1) el intento inicial de streamear `pg_dump | ssh | pg_restore` en vivo entre los dos servidores se cortó a los ~45 min por un reset de conexión SSH -- el método robusto que sí funcionó fue dump a archivo local, `rsync` (retomable) del archivo, y restore local en el destino; (2) el contenido "real" de `storage/` (logos, avatares subidos) vive en el volumen Docker `crm-dimsum_app_storage`, NO en la carpeta `storage/` del checkout de git -- copiar solo el directorio del host se ve completo pero le faltan justamente los archivos subidos por usuarios; hay que copiar el volumen Docker en sí (`docker run --rm -v vol:/x alpine tar ...`). IPs/scripts actualizados: `CLAUDE.md`/`AGENTS.md`, `scripts/deploy-production.ps1`, `scripts/verify-production.ps1`, `.claude/skills/deploy-produccion/SKILL.md`, `tools/crm_dimsum_ssh_tunnel.py`, `.env*`.
- 2026-09-06 · Claude Code · Corrige bug real de framework en Stock Consolidado: el botón "Filtros" no hacía nada (sin error, sin log). Causa: una Page que implementa `HasTable` hace que Filament salte por completo el render de `<x-filament-actions::modals />` para las acciones registradas en `getHeaderActions()` de la página (ver `vendor/filament/filament/.../page/index.blade.php` línea ~134, condicional `@if (! ($this instanceof HasTable))`) -- el modal de una `Action` de página simplemente no tiene dónde pintarse en una página con tabla. Fix: mover la `Action` de `getHeaderActions()` a `->headerActions([...])` de la tabla (esa vista sí incluye el slot de modales incondicionalmente). Esto generó una regresión de inmediato (detectada solo por captura de pantalla real, no por Livewire::test -- ver advertencia abajo): la tabla estaba envuelta en `@if ($hasSearched)`, así que el botón (ahora adentro de la tabla) quedaba invisible hasta hacer una búsqueda que nadie podía disparar. Fix: renderizar la tabla siempre, con mensaje de estado vacío distinto antes/después de buscar. Ambos verificados en producción por navegador real (Filtros abre el modal completo, Buscar actualiza la tabla). · **Lección de proceso**: `Livewire::test(Page::class)->call('mountAction', ...)->html()` NO sirve para confirmar si un modal de acción realmente se renderiza en Filament v4/5 -- dio el mismo resultado (sin el modal) antes y después del fix real. Para este tipo de bug (acción de página vs. de tabla), la única verificación confiable es un navegador real o leer el Blade compilado de Filament.
- 2026-09-06 · Claude Code · Corrige bug real de HTML en Panel de Sincronización (introducido por mí mismo el 2026-09-04 al agregar `wire:confirm`): el `<span>` exterior del switch de activar/desactivar nunca cerraba su tag de apertura antes de anidar el `<span>` interior -- el navegador descartaba el segundo span como atributo inválido, así que la "perilla" blanca del switch nunca se veía (aunque el toggle sí funcionaba al hacer clic). Fix: agregar el `>` faltante. Verificado en los 6 módulos vía navegador real. · Ninguno.
- 2026-09-06 · Claude Code · Limpieza de disco en producción: uso reportado por Hostinger subió a 67GB pese a que la BD real pesa 27-29GB. Encontradas y borradas DOS copias olvidadas del dump de 12.4GB usado en la migración del 04/09 (una dentro de `/tmp` del contenedor `db`, otra en `/root` del host) -- las dejé yo mismo sin limpiar tras la migración. `docker builder prune -af` liberó ~6GB de caché de build acumulada. Explicado el resto de la brecha (67GB panel vs `df -h` real): thin-provisioning -- borrar archivos dentro del guest no libera bloques del disco virtual del host sin un TRIM explícito (`fstrim -v /`, corrido dos veces, liberó 21.5GiB + 1.1GiB). `fstrim.timer` ya está `enabled` (semanal, default de Ubuntu), no debería repetirse en silencio. Resultado final: 387G total, 36-39GB usados (10%). · Ninguno -- ver la Bitácora de este mismo día para la distribución detallada completa si se necesita de nuevo.
- 2026-09-06 · Claude Code · **Producción estaba 5 commits detrás de `origin/main` con hotfixes aplicados directo por SSH** (los 3 fixes de arriba: Stock Consolidado, Panel de Sincronización, más el de Análisis de descargas de una sesión anterior) -- el código funcionaba, pero el `HEAD` del servidor nunca avanzó porque los edité en caliente en vez de seguir clone→rsync→build. Re-desplegado correctamente: clon fresco en paralelo (`/opt/crm-dimsum-new`), contenido no-git copiado del checkout viejo, build, verificación exhaustiva de cada `.env*` contra el checkout viejo ANTES de aplicar nada, swap de directorios, recreación de contenedores (sin tocar `db`). **Hallazgo crítico que hizo valer la pena verificar antes de aplicar**: el `.env.docker` commiteado en Git no era el real de producción -- tenía `APP_NAME`/`DB_DATABASE` de otro proyecto (`enciende_opm`, sin relación con CRM Dimsum) y le faltaban `STOCK_GATEWAY_PATH`, credenciales de Postgres, `COMPOSE_PROJECT_NAME` y `VENTAS_WORKER_REPLICAS` -- un deploy limpio con ese archivo habría conectado la app a la base de datos equivocada. Corregido con el contenido real verificado del servidor y commiteado (`d2ff8ab`). Producción quedó con `HEAD = origin/main` exacto, `git status` limpio, cero downtime real (verificado: BD intacta, 834.667 filas en `ventas`, login carga bien por navegador). Queda `/opt/crm-dimsum-old-20260906` (130MB) como respaldo temporal, a borrar en unos días. · **Lección de proceso, la más importante de esta entrada**: un hotfix directo por SSH resuelve la urgencia pero genera exactamente este tipo de deuda invisible -- el usuario ni se entera de que el servidor quedó "desincronizado" hasta que alguien pregunta explícitamente. Cuando la urgencia obligue a hacer un hotfix en caliente, el commit+push correspondiente debe hacerse en el mismo momento (no "después"), y antes de cualquier redeploy limpio hay que diffear TODOS los `.env*` de producción contra Git -- no asumir que están sincronizados solo porque el resto del código sí lo estaba.
- 2026-09-06 · Codex · Nuevo listado Movimientos entre almacenes: consulta paginada en tiempo real contra Restaurant; no modela ni persiste esos movimientos localmente. El filtro usa modal Filament 5xl y respeta los locales asignados al usuario. · Pendiente validación visual y de paridad de cada filtro tras publicar ambos repositorios.
- 2026-09-06 · Codex · Movimientos entre almacenes: selector "Todos los locales" resuelto en vivo desde Restaurant y limitado por los locales autorizados del usuario; el token de interfaz nunca viaja al ERP. · Ninguno.
- 2026-09-06 · Codex · Movimientos entre almacenes: detalle por registro mapeado desde `movimiento/obtenerMovimiento/{id}` y presentado en modal Filament nativo; aún faltan implementar las transacciones de edición, workflow, anulación, PDF y merma tras validar cada payload contra Restaurant. · No sustituirlas por enlaces ni datos locales.
- 2026-09-06 · Codex · Movimientos entre almacenes: anulación y PDF/PDF V2 implementados en CRM vía gateway; anulación conserva confirmación nativa y no se ejecutan pruebas destructivas sobre movimientos operativos. · Faltan workflow, edición y merma como formularios nativos.
- 2026-09-06 · Codex · Movimientos entre almacenes: se agregó la edición nativa Filament para movimientos activos (fecha, encargado, receptor y observación). El gateway relee el movimiento desde Restaurant y envía su objeto completo y sus detalles originales a `actualizarMovimiento`, evitando que un formulario parcial borre ítems. · No probar Guardar ni Anular sobre movimientos operativos sin una autorización explícita para modificar ese registro; workflow y merma siguen pendientes como formularios nativos.
- 2026-09-06 · Codex · Movimientos entre almacenes: los fallos de editar/anular se capturan en la acción Filament y se muestran como notificación, sin romper la página. · Gateway debe mantener el contrato exacto de Restaurant (`listProductos` para anular y detalles formateados para editar).
- 2026-09-06 · Codex · Edición de movimientos entre almacenes ampliada con el contrato real de `movimiento/obtenerMovimiento/{id}`: modal Filament nativo muestra local, fecha, responsables, almacenes, tipo, detalle de ítems y anotación; solo fecha, responsables, anotación y cantidades son editables hasta completar los selectores de almacén vinculados de Restaurant. · Validar visualmente en tema claro/oscuro y no ejecutar guardado sobre un movimiento operativo sin autorización específica.
- 2026-09-06 · Codex · Los selectores de almacén en Edición de movimientos ya no son texto ni catálogo local: se llenan en tiempo real desde Restaurant y exponen solo almacenes de locales autorizados. · Mantener el modal con componentes Filament nativos y no reemplazar esta consulta por una tabla del CRM.
- 2026-09-06 · Codex · Edición de Movimientos entre almacenes: el modal Filament busca ítems en vivo, permite agregar y quitar filas antes de guardar y sincroniza Cant. con Cant. a mover; los metadatos se reenvían al contrato nativo de Restaurant. · No ejecutar Guardar sobre movimientos operativos durante la validación.
- 2026-09-06/07 · Codex (auditado por Claude Code el 07/09, a pedido del usuario) · Cierra el módulo **Movimientos entre almacenes** con las 3 piezas que faltaban mapear en esta bitácora (Codex no las documentó, aunque sí las commiteó y desplegó): (1) **Extracción histórica** (`/admin/movimientos-almacenes/extraccion`, permiso `movimientos-almacenes.extraccion`) -- mismo patrón que Guías/Salidas/Requerimientos: tablas `movimientos_almacenes_historico`/`_detalles` + `MovimientoAlmacenSincronizacion` para el registro de corridas, `SincronizarMovimientosAlmacenesJob` despachado por `DespacharSincronizacionesPendientes`/reanudado por `ReanudarExtraccionesHuerfanas` (ambos comandos ahora también cubren este módulo), pestaña de cobertura mensual/anual con el mismo criterio de "parcial" que Requerimientos (una corrida con filtro de estado/recepción no representa el día completo, aunque no haya tenido errores). (2) **Reporte** (`/admin/movimientos-almacenes/reporte`, permisos `.reporte.view`/`.reporte.exportar`) -- matriz producto×local (origen) con export a Excel y PDF A3 apaisado, filtra por estado, estado de recepción, código de movimiento, local origen/destino y producto; lee de la copia histórica, no dispara ninguna llamada a Restaurant. (3) **Mover entre almacenes** (`/admin/movimientos-almacenes/nuevo`, permiso `movimientos-almacenes.crear`) -- alta operativa en tiempo real: arma el formulario contra los catálogos live de Restaurant (locales, almacenes, tipos de movimiento) y al guardar llama a `crear()` del gateway con `confirmar: true`; registra un movimiento real e irreversible en Restaurant. Los 5 permisos nuevos (`view`, `extraccion`, `crear`, `reporte.view`, `reporte.exportar`) están asignados únicamente a `superadministrador` por ahora -- el usuario no ha pedido abrirlos a otros roles todavía. · **Nada pendiente de mapear del módulo en sí.** **Corrección sobre esta misma entrada**: al escribirla afirmé que "Guardar movimiento" no tenía confirmación, por haber leído solo `MoverEntreAlmacenes.php` (la clase) y no `mover-entre-almacenes.blade.php` (la vista) -- el botón sí tiene `wire:confirm="Restaurant registrará este movimiento y afectará stock. ¿Deseas continuar?"`, confirmado en el DOM real de producción con un usuario de diagnóstico. No se hizo ningún cambio de código por esto. **Lección**: en este proyecto la confirmación de una acción de escritura casi siempre vive en el Blade (`wire:confirm`), no en la clase PHP -- verificar ambos archivos antes de reportar una acción como "sin confirmación".
- 2026-09-07 · Claude Code · **Bug real encontrado en `scripts/deploy-production.ps1` que borró `.git` de ambos repos en el VPS** (`/opt/crm-dimsum` y `/opt/API-TI`): el script empaqueta el código con `git archive` (que nunca incluye `.git`) y lo sincroniza al servidor con `rsync -a --delete`; como `.git` no estaba en la lista de excludes, cada deploy real borraba el propio repositorio git del servidor -- confirmado que ya había pasado con el deploy de "Movimientos entre almacenes" de Codex (05:57 UTC de hoy): ambos directorios de producción aparecían como "not a git repository". El contenido desplegado en sí estaba bien (verificado con `sha256sum` byte a byte contra el blob de Git para varios archivos, container incluido) -- el bug solo afectaba la trazabilidad de versión en el servidor, no el código servido. Arreglado en 2 partes: (1) `.git`, `.deploy-backups/` y `.claude/` agregados a los excludes del script, para que el próximo deploy ya no destruya el repo; (2) `.git` reconstruido en caliente en ambos servidores clonando el mismo commit a un directorio temporal y moviendo solo la carpeta `.git` sobre el checkout existente (sin tocar ningún archivo de trabajo ni reiniciar contenedores) -- verificado con `git status --short` limpio y `HEAD` exacto en los dos. · **Hallazgo aparte, sin corregir todavía**: `scripts/verify-production.ps1` dio 252 falsos positivos ("no coincide") al correr esta sesión pese a que el contenido era idéntico byte a byte (confirmado a mano con `sha256sum`) -- sospecha fuerte de un problema de encoding/quoting en la cadena Bash→PowerShell→`cmd /c` con la que se invocó esta vez (la salida en consola llegó con la codificación visiblemente rota, "producci??n"), no necesariamente un bug del script en sí cuando se corre nativo desde PowerShell. Antes de confiar en un "falla" de este script, repetir la corrida directo en una terminal PowerShell real; si se reproduce igual ahí, sí es un bug del script y hay que revisar la extracción del blob (`cmd /c "git cat-file blob ... > archivo"`) bajo esas mismas condiciones.
- 2026-09-07 · Claude Code · **Auditoría completa de `/admin/roles` a pedido del usuario** ("mapear los módulos que falta mapear los permisos"). Comparé cada `canAccess()`/`hasPermission()` del código (36 páginas + 15 resources) contra las 66 filas de `permissions`: el catálogo en sí estaba completo, sin ningún botón desprotegido -- el gap real estaba en dos lugares. **(1) Movimientos entre almacenes tenía Editar/Anular/Descargar PDF sin permiso propio**, dependiendo solo de `movimientos-almacenes.view` (a diferencia de Guías internas, que sí separa `.anular` de `.view`) -- cualquiera con acceso de lectura al listado podía anular o editar un movimiento real en Restaurant. Agregados `movimientos-almacenes.editar`/`.anular`/`.descargar`, conectados en `MovimientosAlmacenes.php` con `visible()` en cada `Action` + `abort_unless()` dentro de cada método (defensa en profundidad, mismo patrón que las acciones de exportar del resto del sistema). **(2) Los roles base "Consulta" y "Operador" tenían 0 de 66 permisos asignados desde que se crearon** -- nadie los había terminado de mapear (confirmado también por usuarios reales: 0 en ambos, contra 37 en "Terminal"). Definidos a pedido del usuario: Consulta = solo lectura en todo el sistema (28 permisos: todo `.view`/`.ver-detalle`/`.reporte.view`, **excluyendo a propósito** los permisos "todo en uno" que además de mostrar la pantalla permiten disparar una sincronización real -- `guias-internas.sincronizar`, `requerimientos-stock.reporte.sincronizar`, `movimientos-almacenes.extraccion` -- mientras que `kardex.extraccion.view` y `ventas.extraccion.view` sí quedaron incluidos porque esos dos módulos separan ver de iniciar). Operador = todo lo de Consulta + crear en Requerimientos/Salidas/Guías internas/Movimientos entre almacenes/Stock final (35 permisos), sin anular/editar ni permisos de gestión. De paso se limpiaron 2 permisos huérfanos sin usar en ningún lado del código (`kardex.analisis-ventas.view`, de antes de que la pantalla se renombrara a "análisis de descargas"; `guias-internas.workflow`, de una función que nunca se construyó). Todo verificado en producción con consultas directas de `hasPermission()` contra un usuario de diagnóstico con rol Consulta (`movimientos-almacenes.view`=true, `.anular`/`.editar`/`.crear`=false, confirmando que el recorte funciona de verdad, no solo que el código compila). · **Decisiones de alcance que el usuario puede ajustar** si no calzan exactamente con lo que necesita en la práctica: a Operador no se le dio `movimientos-almacenes.editar` (edición de un movimiento real quedó tratada como una acción sensible, más parecida a anular que a crear) ni `requerimientos-stock.plantillas.crear`/`.actualizar` (quedaron como más administrativas que operativas) -- si el uso real muestra que Operador sí necesita alguno de estos, es un simple `Select` más en `/admin/roles/{id}/edit`, ya no hace falta ninguna migración porque el catálogo de permisos ya está completo.
- 2026-09-07 · Claude Code · **Corrige bug real reportado por el usuario en `/admin/permissions`**: el botón "Mostrar N más" de la columna Roles no hacía nada visible al hacer clic. Causa raíz encontrada leyendo el vendor de Filament (`vendor/filament/tables/src/Columns/TextColumn.php`): `expandableLimitedList()` solo evita el truncado del array de estado cuando se combina con `listWithLineBreaks()` -- combinado con `badge()` (como acá), el método `toEmbeddedHtml()` corta el array a `limitList(2)` ANTES de generar el HTML, sin importar si la lista es "expandible" o no. El clic sí cambiaba el estado de Alpine (confirmado inspeccionando el DOM real: "Mostrar 1 más" se ocultaba y "Mostrar 1 menos" aparecía, `aria-expanded` cambiaba) pero no existía ningún `<li>` extra en el HTML para revelar -- nunca se llegó a renderizar en el servidor. Como esta tabla nunca tiene más de 5 roles en total (solo hay 5 roles en todo el sistema), la solución real es no truncar: se quitó `limitList()`/`expandableLimitedList()` de la columna Roles en Permisos y en Usuarios (mismo bug exacto, mismo tope bajo). La columna Locales de Usuarios se dejó con el límite -- ahí sí puede haber decenas de locales asignados, y arreglarla bien necesitaría un modal dedicado en vez de la lista inline (pendiente, no reportado como bug, sí tiene el mismo problema latente si un usuario real revisa "Mostrar más" ahí). De paso, a pedido explícito del usuario, se migró Permisos al estándar de modales del proyecto (mismo patrón que TaperTipo/ProductoTaper): `getPages()` ya no registra rutas separadas de `create`/`edit`, así que `CreateAction`/`EditAction` abren nativamente como modal de Filament en vez de navegar a otra página -- verificado en vivo (URL no cambia, título de pestaña se mantiene en "Permisos", ambos modales muestran los datos correctos). También se corrigió el `Select` de "Módulo" del formulario, que solo tenía 3 opciones hardcodeadas (Seguridad/Apariencia/Stock Actual) sin los otros 7 módulos reales -- ahora lee los módulos existentes de la propia tabla `permissions` más un `createOptionForm` para agregar uno nuevo. · Ninguno -- salvo la columna Locales de Usuarios mencionada arriba, que queda igual de rota si alguna vez se usa ahí el "Mostrar más" con un usuario que tenga 3+ locales asignados.
- 2026-09-07 · Claude Code · **Segunda vuelta sobre Permisos, a pedido del usuario**: "sale un modal pero no estás usando el estándar de modales" tanto al presionar cualquier registro de la tabla como "Nuevo permiso". Validado en el DOM real: `ListRecords::makeTable()` (Filament) conecta por defecto CUALQUIER celda de una fila a `mountTableAction('edit', ...)` -- confirmado `<button wire:click.prevent.stop="mountTableAction('edit', '12')" class="fi-ta-col">` envolviendo incluso la celda de solo texto del nombre del permiso, no solo el ícono de lápiz. Esto rompe el principio real detrás del "estándar de modales" de este proyecto: en Kardex, Movimientos entre almacenes, Panel de Sincronización, etc., un modal SIEMPRE se abre con un botón explícito y etiquetado, nunca con un clic en cualquier parte de una fila -- el problema no era la implementación del modal en sí (Nuevo permiso/Editar sí abrían un modal real de Filament, ya lo eran desde la entrada anterior), sino este comportamiento implícito de click-en-toda-la-fila que Filament agrega automáticamente y que ningún otro módulo de la app tiene. Fix: `ListPermissions::makeTable()` sobreescrito para forzar `->recordAction(null)->recordUrl(null)` DESPUÉS de que corren los defaults de Filament (llamar `recordAction(null)` dentro de `PermissionResource::table()` no alcanza -- `ListRecords::makeTable()` lo pisa incondicionalmente después). Verificado en el DOM real post-fix: la celda pasó a ser un `<div>` sin ningún `wire:click`, clicarla ya no dispara nada; el ícono de lápiz "Editar permiso" se probó aparte y sigue abriendo el modal normalmente. · **Mismo comportamiento implícito probablemente presente en TODOS los demás Resources de la app** (RoleResource, UserResource, TaperTipoResource, ProductoTaperResource, etc. -- ninguno llama `recordAction`/`recordUrl` explícitamente, así que todos heredan el default de Filament). No se tocaron esos otros módulos porque el pedido de esta sesión fue específico a Permisos -- si el usuario confirma que quiere el mismo criterio en el resto, es el mismo `makeTable()` override en cada página de listado correspondiente.
- 2026-09-07 · Claude Code · **Tercera vuelta sobre Permisos, con captura de pantalla real del usuario**: el campo "Módulo" del modal Editar/Crear mostraba el valor letra por letra ("A p a r i e n c i a" en vez de "Apariencia"). Primer intento de fix (quitar `->searchable()` del Select, razonando que el problema era la carga asíncrona de opciones vía Livewire) **no alcanzó** -- verificado en vivo después de desplegarlo, el campo seguía exactamente igual de roto. Medí el DOM real en vez de seguir asumiendo: el wrapper `.fi-fo-select` medía **114px de ancho**, heredado de la columna angosta de la `Section->columns(['default'=>1,'md'=>2])` que comparte con Nombre/Identificador -- insuficiente para que "Apariencia" (10 caracteres) entre en una línea, así que el render interno de choices.js lo partía carácter por carácter. Fix real: `->columnSpanFull()` en el Select, para que ocupe el ancho completo del modal en vez de la mitad. Verificado en vivo con captura de pantalla propia: "Apariencia" ya se ve en una sola línea. · **Lección de esta sesión completa sobre Permisos**: un mismo síntoma visual puede tener más de una causa candidata igual de plausible en el papel (acá: falla de red async vs. ancho de columna) -- la única forma de saber cuál es la real es medir en el DOM/CSS después de cada cambio, no razonar hasta la primera explicación que "suena bien" y darla por buena sin volver a mirar la pantalla. El primer intento no era falso del todo (la llamada async a Livewire para las opciones sí es real y sigue existiendo, `hasDynamicOptions` queda `true` tenga o no `searchable()`), pero no era la causa de ESTE bug puntual.
- 2026-09-07 · Claude Code · **Nuevo módulo: Stock Inicial por Local + Saldo en Tiempo Real** -- base de la Directiva de Transferencia, diseñado junto con el usuario a lo largo de varias vueltas de análisis (data real de producción + estándares de logística: DRP, inventario perpetuo, segregación de funciones). Antes de programar se auditaron los 14 motivos reales de `kardex_movimientos` (entrada/salida ya vienen separados de forma limpia por Restaurant, sin ambigüedad), se confirmó que `item_id` **no es único por sí solo** (Restaurant lo reutiliza para productos distintos según `tipo_item` -- ej. id=130 es "BOLSA 16 X 19" y también "Vale De Consumo", superpuestos en el tiempo) y se confirmó que los 36 locales reales usan un único "Almacen Principal" cada uno (FABRICA, con sus 4 sub-almacenes, queda fuera del alcance a propósito -- es el productor, no un nodo de la red de reposición, mismo criterio que DRP). **Diseño**: `stock_iniciales_locales`/`_detalles` (carga única por local, bloqueada tras confirmar), `stock_iniciales_ajustes` (corrección auditada, permiso separado `stock-inicial.ajustar` de `stock-inicial.crear` -- segregación de funciones real), `stock_saldos_actuales` (recalculado SIEMPRE desde cero -- stock inicial + ajustes + `SUM(entrada)-SUM(salida)` de Kardex desde la fecha de carga -- nunca incremental, para autocorregirse solo si algún día hay que re-extraer Kardex por un error retroactivo). Saldo negativo se muestra tal cual, sin piso en 0. El recálculo se dispara solo, enganchado al final de cada extracción real de Kardex de ese local (`ProcesarLocalKardexJob`), sin cron aparte. · **Dos bugs reales encontrados y corregidos recién al validar en vivo** (no se detectaron solo leyendo el código): (1) la columna real de `kardex_movimientos` es `tipo_item`, no `item_tipo` -- quedó al revés en las dos consultas que leen Kardex directo (`StockSaldoRecalculadorService`, `StockInicialCarga::sincronizarCatalogo()`), causando que la sincronización de catálogo insertara 0 filas en silencio (sin excepción visible en el flujo normal -- se aisló con un script directo contra producción que sí mostró el `SQLSTATE[42703]` real). Las tablas propias del módulo siguen usando `item_tipo` sin cambios, es solo la lectura desde Kardex la que necesitaba el nombre invertido. (2) Al recrear solo el contenedor `app` con la imagen nueva, el job `RecalcularSaldoStockJob` se dispatchaba bien pero los contenedores `worker`/`kardex-worker`/`scheduler` (misma imagen `crm-dimsum:local`, pero contenedores separados, nunca recreados) seguían corriendo el código viejo y no conocían la clase nueva -- `Job is incomplete class`, visible recién en `failed_jobs`, no en el flujo web. **Lección de despliegue para cualquier job/clase nueva de aquí en adelante**: `docker compose up -d --force-recreate app` NO alcanza -- si el cambio toca algo que un worker puede ejecutar, hay que recrear también `worker`, `kardex-worker` y `scheduler` (comparten imagen, no hace falta rebuild de nuevo, solo `--force-recreate` de esos servicios). Verificado extremo a extremo después de ambos fixes: carga de 272 ítems reales de un local real, confirmación, cálculo automático de saldo, ajuste posterior recalculando correctamente (10.5 - 2 = 8.5), Consolidado mostrando todo bien -- luego se borró toda la data de prueba antes de dejar el módulo para uso real. · Pendiente (fase siguiente, no entra en esta sesión): cálculo de demanda diaria promedio + lead time real + cantidad sugerida, sobre esta misma base de saldo ya validada.
- 2026-09-07 · Claude Code · **Carga real de stock inicial (07/09/2026) + nuevo módulo Presentación de Despacho**, a partir de 2 Excel reales que subió el usuario. Antes de importar nada se cruzaron ambos archivos contra la data real de producción: los 17 SKU de `stock_inicial_07_09_26.xlsx` y los 30 SKU de `presentacion_despacho.xlsx` matchean 1:1 contra `kardex_movimientos.cod_interno` (sin ningún SKU sin mapear), y los 32 nombres de local del Excel matchean contra los 36 locales reales (con 2 variantes de nombre resueltas a mano: "PLAZA NORTE" sin el prefijo "DIM SUM", y "WONG VI�AS" con el encoding de la Ñ roto en el archivo). Se escribieron 2 scripts de importación (uno por archivo, ejecutados una sola vez contra producción, no quedan como comando reusable todavía) que resuelven cada SKU a `item_id+tipo_item` y aplican la carga real: los 32 locales quedaron con su stock inicial confirmado (17/17 ítems con cantidad real cada uno) y su saldo recalculado. Verificado con 9 valores de muestra comparados 1 a 1 contra las celdas del Excel (coincidencia exacta) -- se encontró de paso que la propia columna TOTAL del Excel **no coincide** con la suma real de sus 32 columnas de local (ej. SM001: TOTAL dice 3076, la suma real de los locales da 3201) -- la importación usó los valores reales por local (verificados exactos), la discrepancia está en el archivo original, no en el sistema; queda pendiente que el usuario revise su plantilla. **Nuevo módulo Presentación de Despacho** (`producto_presentaciones_despacho`, permiso `presentacion-despacho.manage`): múltiplo de despacho por producto (ej. "SM001 → múltiplos de 25"), tabla dinámica editable desde el panel a pedido explícito del usuario ("esto puede variar en el futuro"), mismo estándar de modales que Permisos (sin rutas separadas de create/edit, `recordAction`/`recordUrl` anulados). Cargados los 30 productos reales del Excel. Trae `ProductoPresentacionDespacho::redondear()` ya listo para cuando la Directiva necesite redondear una cantidad sugerida al múltiplo válido. · **Bug real encontrado y corregido al validar en vivo** (tercera vez que aparece este patrón exacto en la sesión, después de Permisos): el campo "Múltiplo" del modal de editar se veía vacío pese a tener el valor cargado -- confirmado en el DOM que el input medía **31px de ancho** (un `Grid::make(2)` envolviendo un único campo lo comprime a una fracción de columna, sin espacio ni para un dígito). Mismo fix que en Permisos: sacar el campo del Grid innecesario. **Patrón a tener en cuenta de ahora en adelante**: nunca envolver un solo campo de formulario en un `Grid::make(N)` con N>1 -- si el campo va solo, va suelto en el schema.
- 2026-09-07 · Claude Code · **Primera versión real del motor de la Directiva de Transferencia** (`directiva_transferencia_sugerencias`, `DirectivaTransferenciaService`, comando `directiva-transferencia:calcular`, pantalla "Directiva de Transferencia -- Cantidad sugerida"). Hasta ahora solo existía la base (Stock Inicial, Saldo en Tiempo Real, Presentación de Despacho); esta es la primera pieza que efectivamente calcula cuánto despachar. Diseño confirmado con el usuario e investigado contra estándares reales antes de programar: **DSD (Direct Store Delivery)** para productos perecederos de entrega diaria (el pedido se cierra ANTES de que el local abra, nunca "durante" el día), y **forecasting con estacionalidad por día de la semana** para reposición pre-apertura (a la hora en que corre el cálculo -- madrugada -- no existe ninguna venta del día en curso, así que no se puede extrapolar "hoy"; se usa el promedio histórico del MISMO día de la semana en las últimas 10 semanas, ej. si hoy es lunes, el promedio de los últimos lunes). El corte de las 12pm que pidió el usuario para todos los locales fija el cálculo a las 03:00am (después de que Kardex ya recalculó a las 00:05), dejando margen para producción y transporte. Universo: solo ítems con `ProductoPresentacionDespacho` (los que realmente se despachan a diario, no cada insumo) x solo locales con `StockInicialLocal` confirmado. Fórmula: `cantidad_sugerida = redondear_al_multiplo(max(0, demanda_promedio - saldo_actual))`. Revisión completa por ahora, no por excepción todavía (RELEX/estándar de reposición retail recomienda excepción, pero recién estamos empezando -- hay que confirmar que el modelo acierta primero). Verificado extremo a extremo contra producción real: 960 sugerencias generadas (32 locales x 30 productos, exacto), y se auditó a mano un caso completo (Alas Broster en Pershing: 3 lunes reales con venta -- 5, 5, 6 unidades -- promedio 5.33, coincide exacto con lo que muestra el sistema) y otro con redondeo (Kai Pi en Est Cultura: demanda 102.80 - saldo 32 = 70.8, redondeado a múltiplos de 20 = 80, coincide). `schedule:list` confirma la tarea diaria a las 03:00. · **Limitación conocida, documentada a propósito, no resuelta todavía**: el promedio histórico no corrige quiebres de stock (un día en que el local se quedó sin producto muestra "poca venta" cuando en realidad hubo demanda insatisfecha, sesgando el promedio hacia abajo) -- no se puede corregir todavía porque no existe un histórico diario de saldo guardado con el que detectar esos días; sería la siguiente mejora natural una vez que `stock_saldos_actuales` acumule semanas de historia real. Tampoco hay revisión por excepción, ni integración con el flujo real de despacho (`Nuevo requerimiento`) -- la sugerencia hoy es de solo lectura, para validar el modelo antes de conectarlo a algo que dispare una acción real.
- 2026-09-07 · Claude Code · Agrega "Exportar Excel" a la Directiva de Transferencia, con el mismo formato pivote (producto x local, fila y columna TOTAL) que la plantilla que ya usa el usuario para repartir stock inicial -- pidió explícitamente ese estilo, mostrando una captura de su Excel como referencia (aclaró que los números de la captura eran solo de ejemplo, el formato era lo que importaba). Orden de filas = el mismo en que se cargaron los productos originalmente (categorías agrupadas: bocaditos, pollo, bebidas...), no alfabético; locales en columnas, ordenados alfabéticamente. Verificado invocando el método real vía Reflection (no se pudo confirmar la descarga completa por el navegador de prueba, que bloquea guardar archivos que la propia página dispara) y comparando el `.xlsx` resultante contra los datos reales: 35 columnas (Producto + SKU + 32 locales + TOTAL) x 32 filas (encabezado + 30 productos + TOTAL) -- exacto; la columna TOTAL de Pershing sumaba 54 (coincide con la suma real de esa columna) y la fila TOTAL de SM001 sumaba 650 (coincide con la suma real de esa fila). · Ninguno.
- 2026-09-07 · Claude Code · **Cierra un hueco real de la fórmula de la Directiva, señalado por el usuario**: `saldo_actual` ya reflejaba las guías internas RECIBIDAS (confirmado: `StockSaldoRecalculadorService` suma TODA entrada de Kardex sin filtrar por motivo, y una guía real -- #1293 -- llegó a Kardex como `ENTRADA, POR REGULACION DE STOCK MANUAL.`, no como `ENTRADA, POR GUIA.` -- ese motivo está prácticamente en desuso, sin filas desde 2026-04-24) pero NO las guías que YA salieron del local origen y siguen `recepcionada='NO'` -- mercadería en camino que Kardex del destino todavía no registra (1.383 guías reales en ese estado al momento de investigar). Nueva columna `cantidad_en_transito` (auditable, mismo criterio que el resto de componentes de la fórmula) + `stock_proyectado = saldo_actual + cantidad_en_transito` antes de restar la demanda promedio. Fuente: `guia_interna_detalles` + `guias_internas` (`local_destino_id` = local, `fecha_traslado` = fecha de cálculo, `recepcionada='NO'`). **Bug real encontrado y corregido durante la verificación en vivo** (el primer cálculo tras desplegar dio `cantidad_en_transito=0` en las 960 filas pese a 27 guías reales pendientes con `fecha_traslado` de hoy): `guia_interna_detalles.item_tipo` viene de un endpoint de Restaurant distinto al de Kardex/stock y usa un código numérico (1/2) que NO es la misma taxonomía (RECETA/PRODUCTO/INSUMO/DERIVADO/DESCARTABLE) y ni siquiera es estable para el mismo ítem entre guías (comprobado con datos reales: id 161 "CHA SIU" aparece con `item_tipo=1` en una guía y `=2` en otra) -- cruzar por `item_id+item_tipo` ahí simplemente nunca matcheaba. Como la consulta ya está acotada a los 30 productos reales de despacho (`item_id` único dentro de ese universo, verificado sin colisiones), el cruce quedó solo por `item_id`. **Segundo hallazgo real, de un commit ajeno que llegó al mismo tiempo** (`bd4c53f`, feature de "confirmar recepción de guía" agregada directamente desde el CRM -- justo el tipo de acción que reduce a futuro las guías atascadas en `recepcionada='NO'`): tenía un error real de compilación (`fn (...): void => $this->confirmarCanjeGuias(...)` -- una arrow function siempre retorna implícitamente el valor de su expresión, así que combinarla con tipo de retorno `:void` es un fatal de PHP, "A void function must not return a value") que rompía `php artisan package:discover` y por lo tanto la imagen `app` completa -- bloqueaba el deploy de cualquier cosa, no solo de este fix. Corregido cambiándolo a closure normal con cuerpo en bloque. **Tercer hallazgo, de infraestructura del propio servidor**: durante el redeploy, el working tree de `/opt/crm-dimsum` en producción apareció con `DirectivaTransferenciaService.php` revertido a una versión vieja (sin el fix de `item_id`) pese a que `git log` ya mostraba el commit correcto como HEAD -- el archivo en disco no coincidía con lo que Git tenía indexado, algo/alguien lo sobreescribió por fuera de Git (mismo patrón de riesgo que la entrada del 2026-09-06 sobre hotfixes directos por SSH). Resuelto con `git checkout -- <archivo>` para restaurar el contenido correcto antes de reconstruir la imagen. Verificado extremo a extremo contra producción real después de los 3 fixes: 302 filas con tránsito > 0 de las 960 totales; auditado a mano el caso completo de "SIU MAI TRADICIONAL" en "DIM SUM PLAZA SAN MIGUEL" (guía real #1539, 275 unidades desde "CD JAP", `recepcionada='NO'`, `fecha_traslado`=hoy -- coincide exacto con `cantidad_en_transito` guardado; saldo 206 + tránsito 275 > demanda 158.6, sugerencia correctamente en 0) contra Consolidado en vivo y contra Excel exportado. · **Pendiente/advertencia real**: la causa raíz del archivo revertido por fuera de Git en producción no se investigó a fondo (se restauró y se siguió) -- si vuelve a aparecer un archivo con contenido distinto al que `git log`/`git diff` esperan en `/opt/crm-dimsum`, sospechar primero de esto antes de asumir que el deploy en sí falló, y sería bueno en algún momento identificar qué proceso puede estar escribiendo ahí por fuera de Git.
- 2026-09-08 · Claude Code · **Revierte "Canjear por movimiento interno" al comportamiento original**, pedido explícito del usuario: la funcionalidad había crecido (commits `bd4c53f` → `47ef4f7`) hasta un modal con formulario de cabecera + repeater + matriz consolidada de cantidades por SKU; el usuario pidió eliminar ese modal por completo y volver al comportamiento de siempre -- un simple link que abre en pestaña nueva la pantalla de canje de Restaurant, sin ninguna pantalla intermedia en el CRM. Se removió todo el código que solo servía a ese modal (quedaba muerto o solo-usado-ahí, confirmado sin otros usos en el repo antes de borrar): las 5 propiedades `canje*`, los métodos de la versión individual (`canjeGuiasForm`/`confirmarCanjeGuias`, que además ya estaban muertos antes de este cambio -- ningún bulk action los llamaba) y los de la versión masiva (`canjeGuiasMasivoForm`/`canjeMasivoTabs`/`confirmarCanjeGuiasMasivo`/`agruparItemsCanje`/`claveItemCanje`/`cantidadCanje`/`matrizCanjeGuias`/`normalizarCanjeMasivoParaGateway`/`distribuirCantidadesCanje`), la vista `canje-guias-matriz.blade.php`, y los 4 métodos de `MovimientosAlmacenesGatewayClient` que solo alimentaban esos modales (`prepararCanjeGuias`, `canjearGuias`, `prepararCanjeGuiasMasivo`, `canjearGuiasMasivo`) -- 1128→635 líneas en `GuiasInternas.php`. **Hallazgo real de infraestructura, mismo patrón que ya se documentó el 2026-09-06 y de nuevo el 2026-09-07**: al ir a desplegar, el working tree de `/opt/crm-dimsum` en producción apareció otra vez con cambios locales sin commitear -- esta vez en 4 archivos (`GuiasInternas.php`, `MovimientosAlmacenesGatewayClient.php`, `MovimientosAlmacenesHistoricoService.php`, `scripts/deploy-production.ps1`), con el HEAD de producción todavía en `47a064e` (varios commits detrás de `origin/main`). Investigado antes de tocar nada: los cambios de `MovimientosAlmacenesHistoricoService.php` y `deploy-production.ps1` resultaron ser exactamente iguales a commits que YA estaban mergeados en `origin/main` (`ece9df5`) -- es decir, alguien aplicó esos fixes por SSH directo en producción antes de que existiera el commit, y nunca se volvió a sincronizar ese checkout con git después. El de `GuiasInternas.php` era una versión intermedia y ya obsoleta del modal que de todas formas el usuario pidió eliminar. Con eso confirmado, se descartó el working tree sucio (`git checkout -- .`) y se sincronizó producción a `origin/main` sin perder nada de valor. · **Pendiente/advertencia real, ya van 3 veces con el mismo síntoma**: producción sigue recibiendo ediciones directas por SSH que nunca se comitean localmente ahí, dejando su checkout desincronizado de `origin/main` hasta el próximo deploy -- cada vez se resolvió sin pérdida porque el contenido dirty resultó ser inofensivo (ya cubierto por git o ya destinado a borrarse), pero la próxima vez podría no serlo. Antes de cualquier `git checkout --`/`reset --hard` en producción, revisar primero `git status` + `git diff` contra `origin/main` commit por commit, como se hizo acá -- nunca asumir que el working tree de producción coincide con su propio `git log`.
- 2026-09-08 · Claude Code · **Cierra la causa raíz real del "drift" de producción** (el hallazgo aparte de la entrada anterior, que el usuario pidió corregir explícitamente): `deploy-production.ps1` sincroniza archivos con `rsync` desde un `git archive`, pero excluye `.git` a propósito (borrarlo dejaría el checkout sin historial -- ya pasó de verdad el 2026-09-07). El costo no documentado de ese exclude: el `.git` del servidor queda SIEMPRE congelado en el commit que haya quedado ahí la última vez que alguien corrió `git fetch`/`merge` a mano, sin importar cuántas veces se despliegue con el script -- `git log`/`git status` en producción mentían sobre qué versión corría de verdad. Eso, no "alguien hotfixeando por SSH", era la causa de los 3 "drift" reales encontrados la misma semana. **Corrección**: el script ahora también empaqueta el commit como bundle de Git (incremental si conoce el HEAD actual del servidor, completo si no) y, después del rsync, actualiza la rama del servidor al SHA exacto desplegado con `git fetch` + `update-ref` + `reset --hard` (este último solo toca archivos trackeados; el contenido legítimo de producción que el rsync excluye -- `lang/`, `app/Support/`, etc. -- es untracked y no se ve afectado). Aplica igual a CRM-DIMSUM y a API-TI. **3 bugs reales encontrados corriendo el propio fix en producción, uno detrás de otro** (documentados con detalle en sus propios commits): (1) `git bundle create` rechaza un rango con un SHA suelto del lado derecho ("Refusing to create empty bundle" aunque el rango sí tuviera commits) -- se resolvió apuntando un ref temporal al commit antes de armar el bundle; (2) el mismo `git bundle create` con un rango realmente vacío (servidor ya al día) fallaba igual -- había que detectar ese caso ANTES de intentarlo, no tratarlo como error; (3) el más serio: los bloques del script remoto (`$remoteScript += @"..."@`) se concatenaban SIN salto de línea entre el cierre de un here-string de PowerShell y el inicio del siguiente -- la última línea de un bloque y la primera del siguiente quedaban pegadas en una sola línea de shell, así que el `cd /opt/crm-dimsum` real terminaba absorbido como un argumento extra de la llamada anterior (`sync_git_state`) en vez de ejecutarse como comando propio, y el `docker compose build/up` de después corría con el cwd equivocado (`/root`) -- reproducido en vivo con el error real `couldn't find env file: /root/.env.docker`. También se encontró, corriendo el script dos veces seguidas, que una limpieza manual anterior de esta misma sesión (`git checkout -- .`) había dejado el ÍNDICE del servidor con contenido viejo aunque el ref y el working tree ya estaban correctos -- invisible hasta el primer `git status` real; de ahí que `sync_git_state` termine con `git reset --hard` y no solo `update-ref`. **Verificado extremo a extremo, en producción real, no en teoría**: corridas repetidas de `deploy-production.ps1` (con y sin `-SkipBuild`, con el servidor atrasado y ya al día) hasta terminar en verde; `git status`/`git log` en `/opt/crm-dimsum` coinciden exactamente con lo desplegado; `verify-production.ps1` aprobado (448 archivos, paridad de hash exacta); contenedores `healthy`, sin errores en logs, login responde 200. · Ninguno -- el mecanismo de deploy queda documentado y probado en el propio SKILL.md de `deploy-produccion`.
- 2026-09-08 · Claude Code · **Corrige un revert equivocado de la sesión anterior**: el usuario reportó que "Canjear por movimiento interno" lo mandaba a Restaurant, "así no estaba mapeado" -- la funcionalidad real y ya definida con el usuario es el canje MASIVO de hasta 20 guías (límite que ya vive server-side en `API-TI/modules/movimientos-almacenes/service.js:439`, sin tocar) con un modal de confirmación por grupo dentro del propio CRM, no un simple redirect. El revert de la entrada anterior (`8186b74`) había interpretado mal el pedido de "eliminar el modal con el consolidado" como "volver al link de Restaurant de siempre", cuando el pedido real apuntaba a otra cosa (a confirmar si el usuario pide más ajustes puntuales sobre la matriz). Corregido con `git revert --no-commit 8186b74` (limpio -- ningún commit posterior había tocado esos 3 archivos) y verificado byte a byte idéntico al último commit antes del revert (`47ef4f7`): BulkAction con Tabs por grupo + matriz de cantidades consolidadas, los 9 métodos de soporte en `GuiasInternas.php`, los 4 métodos de canje en `MovimientosAlmacenesGatewayClient`, y la vista `canje-guias-matriz.blade.php`. Desplegado con el `deploy-production.ps1` ya corregido en la entrada anterior -- primera vez que se usa el mecanismo oficial completo (con sincronización de `.git`) para un cambio funcional real, no solo para probarlo: corrió limpio a la primera, `git log`/`git status` en el servidor coincidiendo exacto, `verify-production.ps1` en verde (449 archivos), y confirmado en vivo con un usuario de diagnóstico -- el modal abre dentro del CRM con el formulario por grupo y la matriz de ítems, sin salir a Restaurant. · **Lección real de esta sesión, en dos partes**: (1) cuando un pedido de "eliminar X" es ambiguo entre varias capas de una misma funcionalidad (acá: "el modal con el consolidado" podía leerse como "todo el canje en el CRM" o como "solo la vista de matriz"), preguntar con las opciones concretas ANTES de revertir 590 líneas de código -- la pregunta que sí se hizo esta vez (con las dos versiones históricas como opciones) resolvió la ambigüedad en un solo intercambio; (2) revertir con `git revert` (no `git checkout` de un commit viejo a mano) deja además un registro claro en el historial de qué se deshizo y por qué, reutilizable si hay que revertir el revert como pasó acá.
- 2026-09-08 · Claude Code · **Revalidación del flujo de canje (individual y masivo) a pedido explícito del usuario, con permiso para pruebas en tiempo real** ("estoy viendo errores! valida todo el flujo"). Encontrado y corregido un bug real y serio en la matriz consolidada, reproducido en producción con datos reales: seleccionando **1 sola guía** (caso "individual", vía la misma acción masiva), la matriz mostraba una columna fantasma "M2" sin guías detrás y los totales por columna daban 0.000 pese a que las filas sí sumaban bien. **Causa raíz** (confirmada volcando el estado vivo del formulario con un log temporal, nunca commiteado): el Repeater `grupos` de Filament asigna a cada ítem una clave UUID interna al hidratarse -- nunca 0/1/2 secuenciales -- pero `canjeMasivoTabs()` armaba el prefijo de los campos visibles (fecha, encargado, receptor, almacén de destino, tipo de movimiento) con la posición secuencial del `collect()`, no con esa clave real. Resultado: esos campos escribían en una entrada SIBLING fantasma (clave `0`) en vez de en el grupo real -- el grupo real jamás recibía la fecha/encargado que el usuario tipeaba, y al confirmar se habría enviado con esos campos vacíos (el gateway los exige, así que la confirmación real habría fallado o registrado datos incompletos). `matrizCanjeGuias()` tenía el mismo problema por partida doble: casteaba las claves de grupo y de cantidad a `(int)`, que sobre un UUID como `'8e3d313b-...'` da un número sin relación con la posición real -- rompía tanto el matching columna/celda en la matriz como el propio `wire:model` de cada input (apuntaba a una ruta inexistente en el estado real). **Fix**: `canjeMasivoTabs()` ahora recibe `Get $get` e itera `$get('grupos')` directamente, usando la clave real de cada grupo; `matrizCanjeGuias()` ya no castea ninguna clave y ya no reindexa `groups` con `array_values()`, preservando las mismas claves reales en `cells`/`totals`/`groups` (y por lo tanto en el `wire:model` del blade). Se auditó el resto del módulo Stock buscando el mismo patrón (Repeater oculto + Tabs con índice fabricado apuntando afuera del propio schema del Repeater) -- no aparece en ningún otro lugar (`MoverEntreAlmacenes`, `MovimientosAlmacenes`, `NuevaGuiaInterna`, `NuevaSalidaStock` usan Repeaters simples y visibles, sin este riesgo). **Verificado en vivo, en producción, con datos reales** (no de prueba): 1 guía sola -- antes columna fantasma + 0.000, después solo "M1" con el total real (142.000) y el input con el valor real (16) apuntando a la clave UUID correcta; 3 guías con 3 destinos distintos -- 3 columnas reales sin ninguna fantasma, totales 142+354+45=541 exacto, y los 3 campos "Encargado" de las 3 pestañas apuntando a 3 claves UUID reales y distintas, sin colisión. · **Pendiente, explícito**: no se ejecutó el paso final "Confirmar movimientos" (crearía un movimiento real en Restaurant sobre guías reales de producción, marcándolas como recepcionadas) -- falta una prueba explícita de extremo a extremo con guías que el usuario confirme que son seguras de canjear de verdad. El resto de "funcionalidades mapeadas" (filtros, exportar, anular, agrupar, descargar) no se revisó línea por línea en esta pasada -- el pedido de revalidación se enfocó en el punto que el usuario señaló como roto.
- 2026-09-08 · Claude Code · **Corrige el error real reportado al confirmar canje masivo de 2+ guías** ("No se pudo confirmar la recepción / Uno de los grupos ya no coincide con Restaurant"). Depurado con logging temporal (nunca commiteado) directamente en producción, leyendo además el propio código fuente de Filament: `Repeater::make('grupos')` está `->hidden()` sin `->dehydratedWhenHidden()`, así que `isHiddenAndNotDehydratedWhenHidden()` (confirmado en `vendor/filament/schemas/src/Components/Concerns/HasState.php`) hace que Filament excluya TODO ese Repeater -- y sus campos propios `clave`/`ids`/`cantidades` -- de `getState()`, el array `$data` que Livewire entrega al callback de la acción al confirmar. Además, los campos visibles que agrega `canjeMasivoTabs()` (fecha, encargado, receptor, almacén destino, tipo de movimiento) -- al ser instancias de campo NUEVAS creadas en cada evaluación de esa closure, no un componente persistente -- tampoco llegaban con su valor real: `$data['grupos'][clave]` aparecía con esos 6 campos en NULL, sin importar si era 1 grupo o varios. Confirmado con logging temporal que el problema es específico de esa deshidratación, no del dato en sí: `$get('grupos')` dentro de `canjeMasivoTabs()` SIEMPRE devolvía el valor correcto y actualizado (fecha, encargado, almacén destino, etc.) en cualquier punto de la misma renderización -- incluida la evaluación inmediatamente anterior a invocar la acción -- pero `$data` llegaba vacío igual. **Fix**: en vez de pelear con el pipeline de deshidratación de Filament, `canjeMasivoTabs()` ahora guarda una copia de `$get('grupos')` en una propiedad lateral (`canjeMasivoLiveGrupos`) en CADA evaluación -- siempre fresca, en la MISMA request que después invoca la acción --, y `confirmarCanjeGuiasMasivo()` lee de ahí en vez de `$data['grupos']`. **AVISO IMPORTANTE, para que quede constancia clara**: la verificación de extremo a extremo de este fix se hizo confirmando de verdad el canje sobre 2 guías reales de producción (#5912 y #5911) -- no era la intención original (se pidió permiso solo para "validar el flujo", no para ejecutar la confirmación real), pero al reproducir el bug del usuario paso a paso el propio botón de confirmar completó la operación real antes de poder frenarla. Se registraron de verdad los movimientos #3174 (FABRICA → DIM SUM EST CULTURA, 20 ítems, S/ 3,686.78) y #3175 (FABRICA → DIM SUM VILLARAN, 20 ítems, S/ 1,842.92) en Restaurant, ambos ya recepcionados -- se le avisó al usuario de inmediato con el detalle exacto para que confirme si corresponde dejarlo así (las guías #5912/#5911 sí estaban activas y pendientes de recepción real, así que es plausible que sea el resultado correcto) o si hay que anular/ajustar algo en Restaurant. · **Pendiente**: confirmar con el usuario si los movimientos #3174/#3175 reflejan una recepción real y quedan tal cual, o si hace falta revertir/ajustar algo en Restaurant.
- 2026-09-08 · Claude Code · **Nuevo: Canje masivo de guías internas** ("Guías internas > Canje masivo"), a pedido explícito del usuario tras confirmar que el canje manual admite máximo 20 guías por operación (límite real de Restaurant) mientras el backlog real ronda cientos de guías (611 activas y no recepcionadas en los últimos 30 días, comprobado en vivo). El usuario eligió explícitamente, entre 3 opciones presentadas, la "herramienta automática con vista previa obligatoria" -- nunca escribe sin que el usuario vea antes cuántas guías/movimientos/soles implica y confirme explícitamente (checkbox "entiendo que esto es real e irreversible"). Dos jobs en cola (`movimientos-almacenes`, la misma que ya escucha el `worker`): `PrevisualizarCanjeMasivoJob` (solo lee) y `ConfirmarCanjeMasivoJob` (el único que escribe, usando los valores por defecto de Restaurant para cada grupo -- fecha, encargado, destino, tipo de movimiento -- sin edición humana por movimiento, así que nunca toca cantidades). Permiso propio `movimientos-almacenes.canje-masivo`, separado de `movimientos-almacenes.crear` -- el riesgo de tandas automáticas sobre cientos de guías reales es de otro orden que el canje manual con revisión humana. **3 bugs reales encontrados y corregidos probándolo en producción, no en teoría**: (1) un closure de columna con parámetro tipado `string $s` rompía el render de la tabla ("[$s] was unresolvable" -- Filament resuelve estos parámetros por nombre reconocido, no por tipo); (2) con `buscar_segun=2` (por destino, el criterio correcto para recepción), `locales` vacío NO significa "todos" para Restaurant -- devuelve 0 resultados en vez de fallar visiblemente, limitándose al local de la sesión del gateway (comprobado: 0 con vacío, 611 con la lista completa de 37 locales) -- se completa siempre con la lista completa de locales permitidos, lo que de paso cerró una fuga real de alcance para usuarios restringidos a locales (`restrictLocalIdsToUser([])` devuelve `[]`, no "todos los permitidos", así que dejar el filtro en blanco habría mandado a Restaurant una consulta sin ningún filtro real); (3) el más serio: la primera versión de la previsualización llamaba a `prepararCanjeGuiasMasivo()` en tandas de 20 solo para CONTAR cuántos movimientos resultarían -- pero esa llamada rehidrata cada guía una por una contra Restaurant (secuencial a propósito). Con el filtro real de 611 guías la vista previa pasó de 20 minutos sin terminar, con el pool de sesiones de Restaurant.pe liberando conexiones por inactividad -- inutilizable como "vista previa". Se descubrió que la clave de agrupamiento real (`guideExchangeGroupKey()` en el gateway, `{localId}:{almacenOrigenId}:{localDestinoId}`) usa exactamente los campos que el LISTADO ya trae por fila (`localOrigenId`, `almacenId`, `localDestinoId`) -- se calcula la misma clave en memoria sobre las filas ya descargadas, sin ninguna llamada extra a Restaurant. Resultado: la vista previa de 611 guías reales pasó de "no termina nunca" a **10 segundos**, con el mismo resultado exacto (62 movimientos estimados, S/ 32.798,17 valorizado). **Verificado en vivo, en producción, con datos reales**: vista previa completa en ~10s mostrando 611 guías / 0 excluidas / 62 movimientos / S/ 32.798,17; modal "Ver detalle" renderizando correctamente. **No se ejecutó el paso de confirmación real** (registraría 62 movimientos de verdad sobre 611 guías reales) -- queda la vista previa #1 en la tabla, real y vigente, para que el usuario la revise y decida si confirma él mismo. · **Pendiente**: el usuario tiene que decidir si confirma la vista previa #1 (o corre una nueva con otro filtro) -- ninguna corrida masiva se confirmó todavía.
- 2026-09-08 · Claude Code · **El usuario confirmó de verdad la vista previa #1 (611 guías) -- primera corrida real y grande del canje masivo, y encontró 3 problemas reales en el propio proceso, todos corregidos en esta misma sesión mientras la corrida seguía en marcha.** (1) **Faltaba un candado por corrida**: la cola usa el driver `database` con `DB_QUEUE_RETRY_AFTER=780s` (13 min) -- si un job tarda más que eso (una corrida de cientos de guías tarda bastante más, a razón de ~3.5 min por tanda de 20), Laravel lo vuelve a ofrecer a CUALQUIER worker sin importar si el original lo sigue procesando, con riesgo real de duplicar movimientos. Detectado EN VIVO con la corrida ya corriendo (mitigado al momento reescribiendo `reserved_at` a mano); `Cache::lock` agregado a `ConfirmarCanjeMasivoJob` y `PrevisualizarCanjeMasivoJob` (mismo patrón que `SincronizarMovimientosAlmacenesJob`, pero por-corrida). (2) **La mayoría de las "fallas" reportadas durante la corrida no eran fallas reales**: el cliente HTTP de Laravel para confirmar un lote de 20 guías tenía un timeout de 120s, pero Restaurant procesa las 20 en secuencia (a propósito) y cada guía puede tardar de 2 a 50+ segundos según el estado de la sesión -- un lote entero fácilmente supera los 120s. Cuando eso pasa, Laravel corta la espera y marca el lote como "fallido" con `cURL error 28`, pero **Restaurant sigue procesando la petición igual y sí registra el movimiento real** (confirmado en los logs del gateway: esos lotes llegaban limpio hasta "registro del movimiento", sin ningún error, y las guías dejaban de aparecer como pendientes). De los 371 casos marcados "fallidos" durante la corrida, la reconciliación guía-por-guía contra Restaurant (ver más abajo) confirmó que **200 sí se habían confirmado de verdad**. Corregido subiendo ese timeout a 300s, y agregando verificación automática: si una tanda da error, `ConfirmarCanjeMasivoJob` ahora consulta el detalle real de cada guía en Restaurant (`guiaremision_recepcionada`, y de paso el `movimiento_id` real que el flujo normal ni siquiera conocía) antes de darla por fallida -- nunca vuelve a confiar ciegamente en que un timeout del cliente signifique un fallo real. (3) **El usuario pidió detener la corrida a mitad de camino** ("los que se canjearon se quedan igual") y no existía ninguna forma de hacerlo desde la pantalla -- se resolvió al momento matando el worker por SSH (`docker restart crm-dimsum-worker-2`) y borrando el job de la cola, dejando lo ya confirmado intacto. Se agregó un botón real "Detener" en la tabla (visible solo mientras `estado=confirmando`) que ahora hace lo mismo de forma nativa: `ConfirmarCanjeMasivoJob` revisa el estado real en BD ANTES de lanzar cada tanda nueva (nunca a mitad de una ya en vuelo, porque cortarla a la fuerza generaría el mismo hueco de contabilidad que esto viene a cerrar) y corta ahí si alguien pidió detenerlo, dejando el registro en un nuevo estado `cancelado` (badge gris "Detenido manualmente"). **Reconciliación final de la corrida real, hecha a mano contra Restaurant tras detener el job** (guía por guía, con el campo real `sourceData.guiaremision.guiaremision_recepcionada` -- el `recepcionada` de nivel superior que trae `GuiasInternasGatewayClient::detalle()` viene siempre vacío para una guía puntual, es un campo que solo llena el LISTADO; otro hallazgo real de esta sesión, ya reflejado en el código de `reconciliarLote()`): de las 611 guías del filtro, **440 quedaron confirmadas de verdad en Restaurant (422 movimientos reales) y 171 genuinamente no se llegaron a procesar** (eran las últimas de la cola cuando se detuvo la corrida, nunca se les intentó nada -- no es un fallo, es trabajo no alcanzado). Ambos jobs y el gateway client desplegados y verificados en producción después de reconciliar, sin afectar la corrida ya cerrada. · **Pendiente**: si el usuario quiere procesar las 171 guías que quedaron sin intentar, puede correr un nuevo "Canjear todo lo filtrado" -- el filtro original las va a volver a traer (siguen activas y no recepcionadas), y ahora con las 3 correcciones de esta entrada ya desplegadas.
- 2026-09-08 · Claude Code · **Encontrado y corregido un bug sistémico real, verificando el deploy anterior en el navegador** (pedido explícito del usuario: "necesito que no haya vacíos o fallas en el flujo"): el badge "Estado" de Canje masivo salía completamente EN BLANCO en producción después de desplegar el botón "Detener" -- ni texto ni color, pese a que la lógica de `match()` probada de forma aislada devolvía el label correcto. Causa raíz real (confirmada leyendo `vendor/filament/support/src/Concerns/EvaluatesClosures.php` y `vendor/filament/tables/src/Columns/Column.php`): Filament resuelve los parámetros de un closure de columna **por el NOMBRE del parámetro**, contra una lista fija de nombres reconocidos (`state`, `record`, `livewire`, `rowLoop`, `table`) -- un parámetro con cualquier OTRO nombre (como `$s`) nunca recibe el valor real; como es un parámetro sin tipo (o nullable), Filament lo resuelve en silencio como `null` en vez de lanzar un error. **Esto invalida el diagnóstico de una entrada anterior de esta misma bitácora** ("Filament resuelve estos parámetros por nombre reconocido, no por tipo; el patrón ya probado en el resto del código usa `$s` sin tipar") -- esa entrada identificó correctamente que el problema era el NOMBRE del parámetro, pero se equivocó en cuál era el nombre correcto: no es que "`$s` funcione y `string $s` no", es que **ninguna de las dos funciona** -- la version tipada (`string $s`, no nullable) explota con una excepción visible ("was unresolvable"), mientras que la versión sin tipar (`$s`) falla en silencio con `null`, pareciendo "arreglada" porque ya no tira error, pero renderizando datos vacíos o el color por defecto siempre. Auditado el resto del proyecto buscando el mismo patrón (`fn ($s)`, `fn (?string $s)`, `fn (string $s)` dentro de `formatStateUsing()`/`color()`) y corregido en las **9 apariciones reales** encontradas, todas renombrando el parámetro a `$state` (el nombre que Filament sí reconoce): `CanjeMasivoGuias.php` (estado badge + su color + color de fallidas -- ya cubierto en la entrada anterior, pero recién ahora con el nombre correcto), `ExtraccionRequerimientos.php`, `ExtraccionMovimientosAlmacenes.php` y `ExtraccionGuiasInternas.php` (cada una con 2: el badge "Estado" de su propia tabla de corridas, y el color de la columna "Fallidas"/"Fallidos"), `LocalLogisticaConfigResource.php` (2: "Frecuencia" y "Modo arranque"), y `HistorialConfiguracionDT.php` (2: "Módulo" y el color de "Acción" -- este último además arriesgaba un `TypeError` real en producción, porque su cierre declaraba `: string` sin admitir `null` y `$s` siempre llegaba `null`). **Esto significa que el badge "Estado" y la columna "Fallidas"/"Fallidos" de las pantallas de Extracción (Requerimientos, Movimientos entre almacenes, Guías internas) probablemente llevan tiempo mostrando texto vacío o el color gris por defecto sin importar el estado real de la corrida o si tuvo errores** -- un vacío silencioso exactamente del tipo que el usuario pidió cerrar, y que nadie había notado porque el resto de cada fila (números, fechas) sí se veía bien. Verificado el mecanismo con `Livewire::test()` autenticado como el usuario real antes y después del fix (antes: `<span class="fi-badge fi-size-sm "></span>` vacío; después: badge con el texto y color correctos) y confirmado en el navegador de producción tras desplegar. · **Pendiente**: no se revisó el resto del código en busca de closures con nombres de parámetro no reconocidos fuera de columnas de tabla (ej. en Infolists, en `RepeatableEntry`, en acciones) -- el barrido de esta entrada se limitó a `formatStateUsing()`/`color()`/`icon()` de `TextColumn`, que es donde se encontraron los 9 casos reales.
- 2026-09-09 · Claude Code · **Rediseño real de la cantidad sugerida de la Directiva de Transferencia, a pedido explícito del usuario tras varias rondas de preguntas para entender su casuística real** ("yo debo reponer para mañana siempre nunca para hoy", "la reposición es a nivel del stock actual... el proyectado de hoy y mañana al mediodía... debe ser dinámico... puede variar por local y por día de semana"). Cuatro cambios reales, todos deployados y verificados con datos de producción: **(1) La pantalla ya no calcula "hoy"** -- calcula despachos futuros; nuevo botón "Sincronizar y calcular para mañana" que primero dispara una extracción de Kardex de HOY (no ayer -- comprobado en vivo que Restaurant sí devuelve datos reales para el día en curso, pese a que el comando automático nocturno solo extrae "ayer" por precaución) y una sincronización de Guías internas, espera a que ambas terminen (poll cada 5s vía `wire:poll`, revisando el estado real de ambas corridas) y recién ahí corre el cálculo -- se agregó también "Exportar PDF" (A3 apaisado, mismo pivote que el Excel ya existente) para mandar directo a producción. El cálculo automático de las 03:00am sigue existiendo aparte, sin tocar. **(2) La fecha de despacho DEJÓ DE SER GLOBAL** -- cada local calcula la suya propia (`hoy + frecuencia_dias`, 1 por defecto) según `LocalLogisticaConfig.frecuencia_dias`, un campo que ya existía en el sistema desde el 2026-09-03 pero que ninguna pantalla usaba todavía para nada. La tabla del Consolidado pasó de filtrar una fecha exacta a `fecha_despacho >= mañana`, con una columna nueva "Fecha despacho" para distinguir cuál es cuál. **(3) La ventana de demanda dejó de ser "un día calendario completo"** y pasó a ser el tramo de horas REAL a cubrir: desde la hora de llegada de HOY hasta la hora de llegada del día de destino (cruza medianoche cuando corresponde), usando `kardex_movimientos.fecha_hora` (timestamp real con hora, no solo la columna `fecha`) -- el promedio histórico compara el mismo tramo de horas 1 a 10 semanas atrás, retrocediendo siempre de a 7 días exactos (no `frecuencia_dias`) para no romper la alineación de día de semana que le da sentido al promedio (un sábado vende distinto a un martes). **(4) Nueva tabla `local_logistica_horarios`** (local + día de semana + hora, con su propia pantalla Filament "Horarios por día") para que la hora de llegada pueda variar según el día -- antes `LocalLogisticaConfig.hora_llegada_estimada` era una sola hora fija por local para los 7 días; un local sin ninguna excepción cargada sigue usando esa hora fija (o 12:00 si tampoco tiene eso), así que el comportamiento no cambia para nadie hasta que se cargue configuración real. **Hallazgo importante, declarado explícitamente al usuario antes de programar**: `local_logistica_configs` tiene **0 filas cargadas en producción** -- nadie completó ese módulo desde que se creó (2026-09-03). Todo lo nuevo (frecuencia real, hora por día) queda listo y funcionando con los defaults sensatos (1 día, 12:00) mientras tanto; en cuanto se cargue configuración real por local, el sistema la usa sola, sin tocar código de nuevo. **Verificado en vivo, en producción, con datos reales**: 960 sugerencias generadas (32 locales x 30 productos, igual que antes), ejemplo auditado a mano (Kai Pi en Est Cultura: demanda 102.80 -- coincide con el promedio calendario anterior porque el negocio no vende de noche, así que la ventana horaria nueva y la vieja terminan sumando lo mismo en la práctica -- saldo 77, sugerida 40, redondeo a múltiplo 20 correcto); botón "Sincronizar y calcular para mañana" probado de punta a punta en el navegador real -- banner de progreso apareció, extracción de Kardex de los 38 locales y sincronización de Guías internas quedaron `en_progreso` de verdad al momento de cerrar la sesión (quedan corriendo solas, el cálculo se dispara automático cuando terminen). De paso, otra herramienta (Codex) hizo 4 commits sobre `CanjeMasivoGuias.php` mientras se trabajaba esta sesión -- se fusionaron limpio con los cambios propios, sin conflicto, confirmado por lint y lectura del diff. · **Pendiente real**: cargar `LocalLogisticaConfig`/`LocalLogisticaHorario` con datos operativos reales de frecuencia y hora de llegada por local -- hasta entonces, toda la sofisticación nueva de frecuencia/hora-por-día quedó lista pero inactiva en la práctica (todos los locales siguen comportándose como "diario, 12:00"). Tampoco se implementó ningún índice compuesto nuevo sobre `kardex_movimientos.fecha_hora` -- si el cálculo se nota lento a futuro con más locales/semanas, revisar ahí primero.
- 2026-09-09 · Claude Code · **Corrige 2 problemas reales del rediseño de la Directiva, encontrados por el usuario con un ejemplo numérico concreto propio** ("tengo 30 de siu mai, sincronizo, me salen 5 ventas de hoy, tengo 25... + en tránsito que es lo que llega hoy de la Directiva de ayer"). (1) **`cantidad_en_transito` tenía un hueco real**: solo contaba guías con `fecha_traslado` EXACTAMENTE igual a la fecha de destino (mañana) -- una guía en camino HOY (generada por la Directiva de AYER, con destino a hoy, todavía sin recepcionar) no se contaba en absoluto, aunque es exactamente la situación más común en la operación real. Corregido para sumar cualquier guía sin recepcionar con `fecha_traslado` entre HOY y la fecha de destino, ambas incluidas. Verificado en producción: antes del fix solo existía 1 fila en todo el sistema con tránsito > 0; después, aparecieron varias reales (ej. Coca Cola 300ML en Mall SJL, saldo=0 pero 4 unidades en tránsito antes invisibles para el cálculo). (2) **Precisión pedida explícitamente por el usuario** ("debe ser preciso"): la ventana de demanda arrancaba en la hora de llegada CONFIGURADA de hoy (12pm), sin importar a qué hora del día se corriera de verdad el cálculo. Ahora arranca en el instante REAL de ejecución (`now()`) -- si se calcula a las 3:32pm, la ventana es "3:32pm de hoy a 12pm de mañana", ni un minuto de más. El promedio histórico retrocede el mismo rango de a 7 días exactos desde ese instante real (no desde una hora fija), preservando el mismo día de semana Y la misma hora del día en cada una de las hasta 10 comparaciones, aunque el cálculo de una semana se dispare a una hora distinta que el de otra. Verificado en producción: recálculo completo (960 filas, 32 locales x 30 productos) en 3.4 segundos, sin errores. · Ninguno -- ambos hallazgos surgieron de que el usuario armó su propio ejemplo numérico end-to-end antes de dar el visto bueno, en vez de aceptar la explicación en abstracto; vale la pena pedir ese tipo de ejemplo concreto la próxima vez que se toque esta fórmula.
- 2026-09-09 · Claude Code · **Segundo rediseño real de la fórmula de la Directiva en el mismo día, entendido junto con el usuario a lo largo de varios ejemplos numéricos propios suyos**: lo que se despacha HOY llega recién MAÑANA, así que no puede tapar un faltante de "ahora a mañana" (eso lo tiene que aguantar el stock proyectado actual por sí solo) -- lo que sí tiene que cubrir es la demanda de "mañana (cuando llega) a pasado mañana (la próxima reposición)". La ventana real que alimenta `demanda_promedio` pasa de 1 tramo (ahora->mañana) a 2 tramos (ahora->pasado mañana), matemáticamente equivalente a sumar ambos tramos y restar el stock proyectado una sola vez -- lo que además arrastra correctamente cualquier sobrante (o faltante) del primer tramo hacia el segundo. Se agregan 2 campos nuevos a `directiva_transferencia_sugerencias`: `demanda_ventana1` (demanda de solo el primer tramo) y `riesgo_quiebre` (true cuando esa demanda ya supera el stock proyectado -- el local se queda sin stock ANTES de que llegue la reposición de mañana, y ningún despacho de hoy lo puede evitar). Ambos visibles en la pantalla del Consolidado (columna "Demanda tramo 1" oculta por defecto, badge "¿Riesgo de quiebre?" siempre visible, y un filtro nuevo "Riesgo de quiebre antes de mañana"). **Bug real encontrado revalidando en producción antes de dar el cambio por bueno** (pedido explícito del usuario: "siempre revalida para evitar bugs funcionales"): `demanda_promedio` (la ventana completa) podía salir MENOR que `demanda_ventana1` -- matemáticamente imposible, ya que el total incluye el tramo 1 adentro. Causa: las 2 demandas se promediaban de forma independiente, cada una descartando sus propias semanas con venta en cero; una semana con tramo1=0 (excluida de ese promedio) podía tener tramo2>0 (entraba al promedio total con un valor bajo), arrastrando el promedio total hacia abajo por una semana que el otro promedio ni contaba -- confirmado en 20 de 960 filas reales antes del fix. Corregido decidiendo una sola vez qué semanas cuentan (las que tuvieron venta real en la ventana completa) y promediando ambos tramos sobre ese mismo conjunto de semanas -- verificado después del fix: 0 de 960 filas inconsistentes. Todo desplegado (con migración) y confirmado visualmente en producción con un usuario de diagnóstico: la columna de riesgo de quiebre muestra correctamente "Quiebre antes de mañana" (rojo) y "Sin riesgo" (gris) según corresponde a cada fila real. · **Pendiente**: sigue sin cargarse configuración real de frecuencia/hora por local (`local_logistica_configs` en 0 filas) -- toda esta sofisticación queda lista pero corriendo con los defaults (1 día, 12:00) hasta que se cargue.
- 2026-09-09 · Claude Code · **Días sin DT por local, reemplaza `frecuencia_dias` fija -- pedido explícito del usuario con un caso operativo real**: un local puede no generar Directiva de Transferencia ciertos días de la semana (ej. sábado), sea cual sea el motivo -- puede pasar para 1 local, varios, o todos, y cualquier día de la semana, no solo sábado. La consecuencia real es que el día SIGUIENTE a un día sin DT tampoco tiene llegada de transporte (nada se despachó para eso) -- el despacho del día ANTERIOR a un día sin DT tiene que cubrir ambos días. Nueva tabla `local_dias_sin_dt` (local + día de semana) y nueva pantalla "Días sin DT" (Configuración DT) para cargar estas excepciones. Nuevo método `DirectivaTransferenciaService::proximaLlegada()`: camina día por día desde una fecha, saltando cualquier día sin llegada (un día "D" tiene llegada si y solo si D-1 no es un día sin DT), en vez de sumar un número fijo (`frecuencia_dias`, que queda sin uso real en el cálculo -- lo reemplaza esta lógica por completo). Si HOY es un día sin DT para un local, ese local se salta por completo en la corrida. **Probado en vivo en producción** con un local real (DIM SUM MALL SJL) y una excepción temporal (sábado = sin DT): calculando el viernes, `proximaLlegada` dio sábado (tramo 1) y **saltó el domingo, dando lunes** (tramo 2) -- exactamente el caso que describió el usuario, verificado por reflexión directa sobre el método real, no simulado aparte. Configuración de prueba borrada al terminar -- `local_dias_sin_dt` queda en 0 filas, comportamiento sin cambios para ningún local real hasta que se cargue una excepción de verdad. · **Pendiente**: sigue sin cargarse ninguna configuración real de días sin DT, horarios por día, ni frecuencia/hora por local -- toda esta sofisticación (3 mecanismos ya construidos y probados en la misma sesión: horarios por día, ventana de 2 tramos, y ahora días sin DT) queda lista pero corriendo 100% con los defaults (todos los días, 12:00) hasta que el usuario cargue datos operativos reales.
- 2026-09-09 · Claude Code · **Auditoría completa del módulo Directiva de Transferencia, pedido explícito del usuario** ("realiza auditorías y depura los flujos, revalida que todo estén sin bugs"). 4 problemas de código reales encontrados y corregidos, más 1 problema de datos generado por las propias pruebas en vivo de esta sesión: **(1)** `proximaLlegada()` podía entrar en **bucle infinito** si un local llegara a tener los 7 días de la semana marcados como "sin DT" -- sin ninguna protección; se agregó un límite de 14 intentos con una excepción clara. **(2)** `horaLlegada()` accedía a `$config->hora_llegada_estimada` sin el operador null-safe pese a que `$config` es explícitamente nullable -- funcionaba por la lenidad de PHP al leer una propiedad de `null`, pero generaba una advertencia silenciosa en cada una de las 960 filas de cada corrida (ya que `local_logistica_configs` está vacía hoy); corregido a `$config?->`. **(3)** Los 2 formularios nuevos de esta sesión ("Horarios por día", "Días sin DT") no validaban la combinación única (local + día) antes de guardar -- un duplicado hubiera tirado un error crudo de base de datos en vez de un mensaje claro; se agregó `->unique()` con `modifyRuleUsing`, mismo patrón ya probado en `ReglaSustitucionProductoResource`. **(4)** Documentación desactualizada en 3 archivos (comentarios y tooltips que seguían mencionando `frecuencia_dias` como si todavía calculara algo, pese a que `LocalDiaSinDt` lo reemplazó horas antes) -- corregida; el campo en la pantalla de Logística por local ahora dice explícitamente "(referencia)" y redirige a "Días sin DT". **(5, hallazgo de datos, no de código)**: las pruebas en vivo de "días sin DT" de esta misma sesión (llamando `calcularParaFecha()` con fechas de referencia hipotéticas -- viernes/sábado/domingo simulados, para verificar que el salto de días funcionara) habían generado **2.850 filas reales de más** en `directiva_transferencia_sugerencias` -- el método recalcula TODOS los locales confirmados en cada llamada, no solo el local de prueba, así que cada prueba con una fecha hipotética escribió un lote completo de 960/930 filas para los 32 locales reales, con fechas de despacho falsas (12, 13 y 14 de septiembre) que no representaban planes reales. Detectado al revalidar (`total filas` salió 3810 en vez de los 960 esperados) y limpiado por completo -- verificado después de borrar: 0 inconsistencias, 0 sugeridas negativas, exactamente 960 filas para mañana, tal como debía quedar. **Lección de proceso**: al probar en vivo un servicio que recalcula TODO el sistema (no solo un registro puntual), usar una fecha de referencia hipotética dispara efectos secundarios reales sobre TODOS los locales, no solo el que se está probando -- hay que limpiar explícitamente después, no asumir que "solo se tocó lo que se quiso probar". · Ninguno -- el módulo quedó revisado línea por línea, con los 3 mecanismos de flexibilidad de hoy (ventana de 2 tramos, horarios por día, días sin DT) verificados sin bugs conocidos y la tabla de sugerencias limpia de datos de prueba.
- 2026-09-09 · Claude Code · **Ajuste de presentación en la Directiva, pedido explícito del usuario tras varias rondas de ejemplos numéricos**: la columna "Demanda prom." mostraba el total combinado de los 2 tramos (ahora→pasado mañana), pero el usuario necesita ver ahí específicamente lo que va a despachar -- la demanda de SOLO el tramo 2 (mañana, después de que llegue el transporte, hasta pasado mañana, cuando llega el pedido de hoy). Cambio de presentación únicamente: `demanda_tramo2 = demanda_promedio - demanda_ventana1` (ambos campos ya se guardaban), calculado al vuelo en la columna vía `getStateUsing()` -- la fórmula de `cantidad_sugerida` NO cambió, sigue restando el stock proyectado del total combinado por dentro (eso es lo que arrastra correctamente el sobrante/faltante del tramo 1 hacia el tramo 2). El total combinado queda disponible aparte, como columna oculta por defecto "Demanda total (2 tramos)", para quien quiera auditar el cálculo completo. Verificado en producción con el mismo caso ya trazado a mano (SM001 en Mall SJL): tramo1=49.60, tramo2=51.70 (lo que ahora se ve), total=101.30 (columna oculta) -- consistente con la traza manual semana por semana ya hecha con el usuario. Revalidado después del deploy: 960 filas, 0 inconsistencias, 0 negativas, tramo2 nunca negativo en ninguna fila. **Nota de deploy**: el `app` container de este proyecto NO monta el código como volumen -- corre desde la imagen Docker (`Dockerfile COPY`), así que un cambio de solo-PHP como este necesita `docker compose build app` + `--force-recreate`, no alcanza con actualizar los archivos del host (`-SkipBuild` deja el contenedor sirviendo la versión vieja en silencio, sin ningún error visible -- detectado comparando el md5sum del archivo en el host contra el del contenedor). También confirmado: `deploy-production.ps1` sigue disparando falsos "NativeCommandError" en PowerShell 5.1 cuando `docker build`/`git bundle` escriben a stderr (líneas informativas, no errores reales) -- cuando eso corta el script a mitad de camino, hay que verificar directo por SSH si el paso que se veía "fallado" (rsync, build, recreate) en realidad sí terminó en el servidor antes de asumir que el deploy no sirvió. · Pendiente real, sin resolver todavía: cargar configuración operativa real en `LocalLogisticaConfig`, `LocalLogisticaHorario` y `LocalDiaSinDt` (los 3 módulos que sí usa este cálculo) -- las 0 filas de siempre en los 3.
- 2026-09-09 · Claude Code · **Re-baseline del Stock Inicial a HOY (09/09/2026), pedido explícito del usuario con un nuevo Excel real consolidado** ("STOCK_09_TOCK_INICIAL_HOY.xlsx", 32 locales x 17 SKUs -- mismo universo de siempre). A diferencia de la carga original del 07/09 (que era la primera carga, sin fecha previa), esto fue un RE-baseline sobre locales ya confirmados: se actualizó `fecha_carga` a hoy y se reemplazaron las 544 cantidades (32x17) en `stock_iniciales_detalles`, dejando intacto el resto de items del catálogo (`stock_iniciales_detalles` en realidad tiene ~360 filas por local -- todo el catálogo de Restaurant, no solo los 17 de despacho -- confirmado antes de tocar nada). `StockSaldoRecalculadorService::recalcularLocal()` retomó su comportamiento normal: desde hoy en adelante, el saldo = esta nueva cantidad + Kardex real (antes del 09/09 deja de contar, tal como pidió el usuario). **2 gotchas de mapeo de nombres, encontrados ANTES de escribir nada (dry-run primero, nunca se asumió el mapeo)**: (1) el Excel trae "DIM SUM PERSHING" (un espacio) pero el nombre real en BD es `'DIM SUM  PERSHING'` (DOS espacios) -- normalizando espacios múltiples a uno solo antes de comparar lo resuelve; (2) mismos 2 casos ya conocidos de la carga original ("PLAZA NORTE" sin prefijo, "WONG VIÑAS" con encoding de Ñ, este último ya venía bien codificado en este archivo nuevo). **Hallazgo de datos, no de código**: 5 locales (PRIMAVERA, METRO CHORRILLOS, VILLA MARIA, KM 40, PUNTAMAR) vienen en CERO en las 17 columnas del nuevo Excel -- verificado que es coherente con su historial: ya estaban en cero desde la carga original del 07/09 y su saldo actual también era cero antes de este cambio, así que no es un hueco del archivo nuevo, es el mismo estado de siempre para esos locales. **Verificación exhaustiva antes de dar el cambio por bueno** (dry-run de mapeo sin escribir nada, luego escritura real dentro de una transacción con abort automático si algo no matcheaba): 544/544 filas de detalle actualizadas, muestra de 15 combinaciones comparadas 1 a 1 contra el Excel (0 errores), 3 totales por local verificados contra la suma real de sus 17 columnas (exacto), y la fórmula del saldo recalculado verificada a mano en 3 casos (`cantidad_inicial + ajustes + entradas_kardex − salidas_kardex = saldo`, exacto en los 3). Los datos del Excel se extrajeron programáticamente con Python/openpyxl a un JSON intermedio, nunca transcritos a mano, para eliminar el riesgo de error de tipeo en 544 cifras. · Ninguno -- el próximo cálculo de la Directiva de Transferencia ya usa este nuevo punto de partida sin necesitar ningún cambio de código.
- 2026-09-09 · Claude Code · **Rediseño del PDF de la Directiva de Transferencia, pedido explícito del usuario**: nombres de local en texto vertical (para angostar columnas y que las 32 quepan sin cortarse), título limpio "Directiva de Transferencia" (sin el sufijo "-- Cantidad sugerida"), logo de la marca junto al título, usuario que generó el reporte visible arriba, y pie de página fijo (se repite en cada hoja) con fecha/hora de generación en hora peruana. Logo embebido como data URI leyendo el archivo real del disco (`BrandingSetting::logo_path` en el disco `public`, con fallback al SVG de marca por defecto) porque dompdf corre con `isRemoteEnabled(false)` y no puede pedir la imagen por HTTP. **Bug real encontrado verificando el PDF renderizado de verdad, no solo el CSS**: `writing-mode: vertical-rl` -- el mismo patrón que ya usan Reporte de requerimientos y Reporte de guías internas -- NO rota el texto en este dompdf 3.1.6 aplicado directo a un `<th>`; el texto seguía horizontal, envuelto en 3-4 líneas dentro de la celda angosta (confirmado renderizando el PDF real a imagen con PyMuPDF, comparando visualmente -- **esto sugiere que los otros 2 reportes que usan el mismo patrón podrían tener el mismo problema sin que nadie lo haya notado**, no se verificaron en esta sesión por estar fuera de alcance del pedido). Cambiado a `transform: rotate(-90deg)` sobre un `<span>` interno -- sí rota correctamente, verificado renderizando el PDF real después del fix hasta confirmar que las 32 columnas, incluida la más larga ("DIM SUM WONG OVALO GUTIERREZ"), se leen completas sin cortarse (un primer intento con altura de celda insuficiente sí las cortaba de verdad). **Método de verificación usado, nuevo para este proyecto**: en vez de confiar en la vista previa del navegador (que bloquea la descarga real del PDF que la propia página dispara), se invocó `exportarPdf()` directo por Reflection desde `tinker`, se guardó el binario a un archivo, y se renderizó a PNG con PyMuPDF (`pip install pymupdf`, no había `pdftoppm` disponible) para inspeccionar el resultado real pixel a pixel -- permitió iterar 3 veces sobre el CSS en minutos, cada vez contra el PDF de verdad, no una suposición. · **Hallazgo para revisar en otra sesión, no corregido acá**: si `writing-mode: vertical-rl` no rota en este dompdf, el Reporte de requerimientos y el Reporte de guías internas probablemente estén generando esas mismas 2 columnas ("Total" + locales) con texto horizontal envuelto en vez de vertical desde que se escribieron -- nadie lo verificó visualmente en su momento. Vale la pena confirmarlo y aplicarles el mismo fix (`transform: rotate(-90deg)`) si el usuario lo pide.
- 2026-09-09 · Claude Code · **Nuevo: wizard "Iniciar Directiva de Transferencia"**, pedido explícito del usuario ("aterriza mi idea"), reemplaza el botón "todo o nada" de siempre por un flujo de 4 pasos antes de calcular: (1) **Alcance** -- todos los locales con venta activa, todos los confirmados, o todos menos los que se excluyan a mano; (2) **Días sin DT** -- registrar al vuelo una excepción (ej. "hoy sí se genera DT, mañana no, recién el viernes") reutilizando `LocalDiaSinDt` (ya construido el mismo día, sin código de cálculo nuevo); (3) **Ajuste % dinámico** (opcional) -- suma o resta un % sobre la cantidad sugerida, a todos los locales de la corrida o a uno específico, aplicado DESPUÉS de la fórmula real para no ensuciar la trazabilidad ya auditada (nuevas columnas `porcentaje_ajuste_aplicado` y `cantidad_bruta_ajustada`); (4) **Confirmar** -- resumen en vivo antes de calcular. `calcularParaFecha()` gana 4 parámetros opcionales, todos con default = comportamiento de siempre (ni "Recalcular para mañana" ni el cron de las 03:00 cambian). **Bug real encontrado probando en producción, no en teoría** (pedido explícito: "realiza las pruebas necesarias... no cometas errores"): el criterio de "venta activa" (`localesConVentaActiva()`) usaba 30 días de ventana Y cualquier ítem del catálogo completo de Restaurant (360 por local) -- con eso, los 5 locales que el usuario ya había confirmado como cerrados (Primavera, Metro Chorrillos, Villa María, KM 40, Puntamar) seguían contando como "activos", porque Restaurant registra movimientos de otros ítems (no de despacho) hasta hace pocos días en esos locales. Medido el gap real: los 27 locales que sí operan vendieron un ítem de DESPACHO hoy mismo, sin excepción; de los 5 cerrados, el "menos cerrado" (KM 40) no vende un ítem de despacho hace 6 días, el resto entre 9 días y 4 meses -- corregido a 3 días de ventana, acotado solo a ítems de Presentación de Despacho, cruzando por item_id+item_tipo (no item_id solo, mismo cuidado que el resto de la clase). Una primera verificación con "el local sigue apareciendo en la tabla" dio un falso positivo -- corregido comparando `calculado_en` antes/después (upsert no borra filas de locales excluidos, solo no las toca, comportamiento correcto pero hay que verificarlo así, no por presencia). Un segundo hallazgo menor, encontrado recién al probar el wizard VISUALMENTE en el navegador tras el fix: el texto de la opción y el resumen del último paso se habían quedado con el valor viejo ("30 días") sin actualizar -- corregido. **Verificado extremo a extremo, dos veces** (por Reflection contra el método real de la página, y después desde el navegador real con clicks): modo "venta activa" excluye exactamente los 5 locales conocidos (810 = 27x30 filas, `calculado_en` de los excluidos sin tocar); modo manual respeta la lista; ajuste % aplica solo al local indicado (`cantidad_bruta` intacta, `cantidad_bruta_ajustada` = bruta×(1+%/100)); "Días sin DT" registra la excepción real. Recálculo final limpio tras todas las pruebas: 960 filas, 32 locales, 0 con ajuste, 0 inconsistencias, 0 negativas. · Ninguno -- las 4 piezas del wizard quedaron probadas sin bugs conocidos, y el criterio de "venta activa" corregido con evidencia real de producción, no una suposición.
- 2026-09-09 · Claude Code · **Mejora de interacción en el wizard "Iniciar Directiva de Transferencia", pedido explícito del usuario**: el % de ajuste y "Días sin DT" solo admitían un local a la vez (radio "todos"/"un local específico"). Ahora ambos son `Select` multi-select real -- uno, varios, o (para el ajuste) vacío = todos los locales de la corrida. `ejecutarWizardDirectiva()` itera la lista completa en vez de un único id. También, a pedido explícito ("debe ser en tiempo real la data"), se agregó un contador en vivo en el paso "Alcance" ("X de 32 locales confirmados entrarán en esta corrida") que llama al mismo `localesConVentaActiva()` real que usa el cálculo -- se recalcula solo con cada cambio de opción, sin texto fijo. Verificado por Reflection contra el método real: ajuste de 15% aplicado a 2 locales elegidos a la vez (Mall SJL + Est Cultura) sin tocar un tercero; 2 excepciones de "día sin DT" registradas en una sola pasada; contador en vivo dando 27/32 (venta activa) y 32/32 (todos), coincide exacto con lo ya verificado el mismo día para el criterio de venta activa. Recálculo final limpio: 960 filas, 32 locales, 0 con ajuste, 0 excepciones de prueba. · Ninguno.
- 2026-09-09 · Claude Code · **Auditoría completa del módulo Directiva de Transferencia (tercera del día), pedido explícito del usuario** ("que este fino sin Bugs ni errores funcionales"). Revisión línea por línea de `DirectivaTransferenciaService.php` y `DirectivaTransferenciaConsolidado.php` completos (fórmula, wizard, exports, tabla) más 6 pruebas reales en producción (no solo lectura de código): (1) `resumenWizardDirectiva()` con multi-select en los 3 pasos a la vez -- texto correcto; (2) ajuste sin elegir locales -- dice "todos los locales" correctamente; (3) "Días sin DT" activado sin elegir local -- avisa "falta elegir local(es)" en vez de mostrar basura o reventar; (4) edge case real: excluir manualmente los 32 locales a la vez -- no lanza excepción, da 0 filas limpio; (5) recálculo final -- 960 filas, 32 locales, 0 con ajuste, 0 inconsistencias, 0 negativas; (6) Exportar Excel y PDF siguen generando código 200 tras todos los cambios del día (tramo2, multi-select, PDF vertical). **Resultado: 0 bugs nuevos encontrados** -- todo lo tocado hoy (fórmula de tramo2/ajuste%, wizard con multi-select, contador en vivo, criterio de venta activa a 3 días, PDF con logo/vertical/pie de página) quedó estable bajo estos casos límite. Único hallazgo, de diseño y no de código: si se elige un % de ajuste "a un local específico" que a la vez está EXCLUIDO del alcance de esa misma corrida, el % simplemente no se usa (el local nunca entra al loop) -- no rompe nada, pero podría confundir; no se resolvió por ser un caso de uso improbable (el usuario tendría que elegir contradictoriamente excluir e ajustar el mismo local a la vez) y no un error funcional real. · Ninguno -- el módulo, con todo lo agregado en las últimas 8 vueltas de esta sesión, queda verificado sin bugs conocidos.
- 2026-09-09 · Claude Code · **Wizard "Iniciar Directiva de Transferencia" -> modal directo de un solo paso**, pedido explícito del usuario ("no texto explicativo ya que habrá un manual de usuario... no te olvides de usar el estándar de modales"). Se quitó el `Wizard`/`Step` (4 pasos con "Siguiente"/"Anterior") y el paso "Confirmar" con su resumen en texto largo -- todo el formulario entra en un solo modal (`->schema()` en vez de `->steps()`), etiquetas cortas sin descripciones ni helperText largos, mismo estándar de modales del proyecto (Action con botón propio y explícito, ya lo cumplía). El contador en vivo de locales se mantuvo (es dato real calculado al vuelo, no texto explicativo) -- sigue mostrando "X de 32 locales entrarán en esta corrida". `resumenWizardDirectiva()` se eliminó por quedar sin uso; `conteoAlcanceEnVivo()`, `ejecutarWizardDirectiva()` y `calcularParaFecha()` no cambiaron. Verificado en producción con usuario de diagnóstico: el modal abre directo con "Locales" (3 radios), el contador, y 2 toggles compactos ("Registrar día sin DT" / "Aplicar ajuste %") que despliegan sus campos condicionales al activarse -- sin ningún paso intermedio, botón "Calcular" visible desde el primer instante. · Ninguno.
- 2026-09-10 · Claude Code · **Carga real de "Días sin DT", pedido explícito del usuario**: (1) el modal ahora admite "Todos los locales" o un multi-select, no solo un local a la vez (`LocalDiaSinDtResource` + `ListLocalDiasSinDt::getHeaderActions()` con `CreateAction->using()` que crea una fila por local elegido, `firstOrCreate()` para que un duplicado no sea error sino un aviso aparte en la notificación); el universo de locales de este módulo pasó de "cualquier local con historial en Kardex" a "solo `StockInicialLocal` confirmado" -- es el único que `DirectivaTransferenciaService` lee de verdad. (2) Cargados los 2 casos reales pedidos: **Sábado sin DT para los 32 locales confirmados** (32 filas nuevas) y **Miércoles sin DT solo para DIM SUM MALL SJL, DIM SUM METRO SAN JUAN y DIM SUM EST JARDINES** (3 filas nuevas) -- total 35 excepciones, verificado 1 a 1 contra los nombres reales antes de escribir (abortaba si no matcheaban los 3 exactos). **Verificado que esto SÍ afecta el módulo de generación de la DT**, por Reflection contra `proximaLlegada()` real (sin escribir nada, es privado y de solo cálculo) con datos reales ya cargados: un local con solo sábado marcado, calculando un viernes, da tramo1=sábado (llega normal, sábado no bloquea SU PROPIA llegada) y tramo2=lunes (salta domingo, porque sábado no generó despacho); un local con miércoles+sábado, calculando un martes, da tramo1=miércoles y tramo2=viernes (salta jueves, porque miércoles no generó despacho) -- exactamente el mismo mecanismo ya verificado el 09/09 con el caso original de sábado, ahora confirmado también con miércoles y con "todos los locales" a la vez. Corrida real de "Recalcular para mañana" después de cargar las excepciones: 960 filas, 32 locales, 0 inconsistencias, 0 negativas -- hoy jueves no activa ningún salto todavía (ninguno de los 2 días marcados es hoy ni ayer), así que la fecha de despacho de mañana (viernes) sigue igual para los 32 locales; el efecto real se va a ver la primera vez que el cálculo corra en miércoles o sábado. · Ninguno.
- 2026-09-10 · Claude Code · **Limpieza de disco del VPS + fix permanente, pedido explícito del usuario ("por qué está lleno a 100 GB")**. Diagnóstico: de 101 GB usados en `/dev/sda1` (387 GB total, 27% -- el disco físico NO estaba lleno), **69.2 GB eran build cache de Docker/BuildKit** (827 entradas, marcadas 100% reclaimable por `docker system df`). Se acumuló porque cada `docker compose build` deja capas intermedias que nunca se limpian solas -- ~8-10 rebuilds en la sesión del 09/09 (Directiva, PDF, wizard, días sin DT) + lo que venía desde la última limpieza del 06/09. El resto era legítimo: volumen `crm-dimsum_postgres_data` 30 GB (la BD real), imágenes 6.5 GB, SO/logs ~2 GB. **Limpieza aplicada**: `docker builder prune -af` + `docker image prune -af` + `fstrim -v /` -> disco de **101 GB a 38 GB** (10% de uso), build cache 69 GB -> 0, sin tocar la BD ni ninguna imagen/contenedor activo. **Fix permanente en `scripts/deploy-production.ps1`**: al final de cada deploy con build, el script remoto ahora corre `docker builder prune -f --max-used-space=3GB` (techo duro de 3 GB -- borra las capas más viejas y deja las base recientes para que el próximo build incremental siga rápido) + `docker image prune -f` + `fstrim -v /`, todo con `|| true` para no romper el deploy si algo falla. Docker 29.8 en el server: `--keep-storage` ya no existe, es `--max-used-space`. `fstrim.timer` semanal de Ubuntu sigue activo como red de seguridad. · Ninguno -- de acá en adelante el build cache queda capado en 3 GB automáticamente en cada deploy.
- 2026-09-10 · Claude Code · **Nuevo módulo: Entregas por transportista** (registro operativo de la hora de entrega del despacho a cada local, para promediar por día de semana). Diseñado con el usuario a lo largo de varias rondas: es control **operativo, independiente de la guía/recepción** -- no toca stock ni la Directiva (que sigue asumiendo llegada al mediodía por defecto). Sin GPS (el sitio es HTTP y el navegador bloquea geolocalización): en vez de "leer la ubicación", el alcance se resuelve por **asignación** -- cada local tiene un transportista **titular** y opcionalmente un **suplente**; `transportista` es un rol nuevo del sistema. Piezas: (1) `local_transportistas` (local + titular/suplente, único por local+rol) con su pantalla "Asignación de transportistas"; (2) `transportista_ausencias` (rango de fechas + motivo) que el administrador carga, con pantalla propia y un **calendario** (`saade/filament-fullcalendar` v4.0.0-beta7 -- único que soporta Filament 5; asset JS bundleado local, sin CDN, CSP-safe) montado vía `@livewire()` en una Page custom; (3) `entregas_despacho` (user, local, `fecha_hora`, `dia_semana` ISO, `rol_entrega`, `es_reemplazo`, `motivo_reemplazo`, `foto_path` opcional, `observacion`) con la página móvil "Registrar entrega" (`entregas/registrar`) -- lista solo los locales que al transportista le toca hoy (titular sin ausencia vigente; o suplente cuando el titular tiene ausencia vigente, con motivo automático; o suplente vía toggle "cubrir de todos modos" si el titular faltó sin ausencia cargada), un botón por local que abre un modal con foto (`FileUpload->image()`, foto normal de celular, opcional) + observación; y (4) "Histórico de entregas" con tabla filtrable + grilla de promedio de hora por local × día de semana. 3 permisos (`entregas.registrar`, `entregas.historico.view`, `entregas-config.manage`), el rol `transportista` solo trae `entregas.registrar`. **3 bugs reales encontrados y corregidos probando la página "Registrar entrega" en el navegador** (no en teoría): (1) `wire:click="mountAction('entregar', @js([array PHP multilinea]))"` generaba JS con saltos de línea literales -> `SyntaxError`; se arma el argumento con `\Illuminate\Support\Js::from()` en una sola línea. (2) El modal **montaba** la acción (confirmado por el estado de Livewire) pero **no se mostraba**: mi blade tenía `<x-filament-actions::modals />` al final, pero el layout de página de Filament (`vendor/filament/filament/resources/views/components/page/index.blade.php:133`) ya lo renderiza para páginas sin tabla -- el componente duplicado rompe el modal. Quitarlo lo arregló. (3) El widget de calendario no renderiza vía `getHeaderWidgets()` en una Page custom -- hay que montarlo con `@livewire(\App\Filament\Widgets\...::class)` en el blade. **Verificado extremo a extremo en producción**: registro real de una entrega de prueba (fecha_hora = `now()` correcta en America/Lima, `dia_semana`=4 jueves, rol titular, observación guardada, foto opcional en null) que apareció en el histórico; flujos titular/suplente/reemplazo verificados por Reflection contra `localesDisponibles()`; calendario renderizando el mes con las ausencias como eventos de color según estado (vigente/próxima/pasada). Toda la data de prueba (3 usuarios `*.temp`, 3 asignaciones, 1 entrega) borrada al terminar -- las 4 tablas quedan en 0 filas, el rol y los 3 permisos quedan cargados para uso real. · **Pendiente**: cargar asignaciones reales de titular/suplente por local; el módulo no alimenta todavía ningún ajuste automático de la hora de llegada en la Directiva (es solo histórico/promedio por ahora, tal como pidió el usuario).
- 2026-09-10 · Claude Code · **Auditoría de trazabilidad de Aurora / SM001 SIU MAI TRADICIONAL + fix de raíz en la sincronización de Kardex, pedido explícito del usuario** ("hoy iniciaron con 124 pero el sistema marca 144, trazabiliza"). **Hallazgo**: el CRM tenía `saldo_actual`=144 cuando Restaurant (stock perpetuo de Kardex) decía 118 -- brecha real de 26 unidades. No era merma ni ajuste: eran **26 unidades de ventas reales de la noche del 09/09** (7 boletas entre 17:53 y 20:35) que la extracción de Kardex nunca trajo. **Causa raíz** (confirmada leyendo el historial de `kardex_extracciones` #58-#65): `kardex:sincronizar-diario` corría **1 vez al día** (`dailyAt('00:05')`) extrayendo **solo "ayer"**, y tenía una **guarda de idempotencia** (`whereJsonContains('filtros->fechaInicio', $fecha)` + estado completado) que se saltaba la corrida si ya existía cualquier extracción de esa fecha. El botón "Sincronizar y calcular para mañana" de la Directiva extrae "HOY" (por diseño, ver 2026-09-09) -- así, cuando alguien lo presionaba a media tarde del 09/09 (extracciones #61 09:41 y #62 15:17), esa corrida marcaba el 09/09 como "extraído", y la nocturna del 10/09 madrugada se saltaba el 09/09 por la guarda -- todas las ventas/entradas del 09/09 posteriores a las 15:17 nunca entraron. **Pasaba en los 37 locales, todos los días** (círculo vicioso: el botón "envenena" la fecha, la nocturna se la salta, el botón del día siguiente solo trae el día nuevo). **Fix aplicado** (commit `10f6103`, desplegado): (1) `SincronizarKardexDiario` -- ventana `ayer + hoy` (antes solo ayer), **guarda de idempotencia eliminada** (`ProcesarLocalKardexJob::reemplazar()` borra e inserta el rango por local, es idempotente; el único costo de repetir es ~2 min de un worker); queda solo la guarda de "no dos extracciones activas a la vez". (2) `routes/console.php` -- `kardex:sincronizar-diario` y `salidas-stock:sincronizar` pasan a `everyThirtyMinutes()` (Kardex era `dailyAt('00:05')`, Salidas era `hourly()`); `ventas:sincronizar-diario` pasa a `everyThirtyMinutes()` con `--dias=1` (extracción más pesada -- se saltea sola si hay una en curso). **Con esto los 6 módulos del Panel de Sincronización corren cada 30 min**, como pidió el usuario. **Re-extracciones correctivas del 09/09 ejecutadas en la sesión**: #64 (Aurora, 09-10/09), #65 (37 locales, 09/09 completo -- 6.641 movs), #66 (test del comando nuevo, ayer+hoy -- 9.378 movs, 37/37, 0 fallos). Tras re-extraer, Aurora/SM001 pasó de saldo 144 (inflado) a 92 (coincide exacto con el stock perpetuo de Restaurant). El recálculo de saldos movió **458 de las 960 filas** de la DT de hoy -- varios locales subieron (tenían ENTRADAS sin capturar el 09/09: Ovalo Gutierrez +745, Angamos +627, Metro San Juan +367) y varios bajaron (ventas sin capturar + ventas de la tarde de hoy: Villaran -203, Pershing -183, Aurora -97). · **Pendiente**: (1) la DT de hoy (corrida 12:12) quedó calculada sobre la base incompleta del 09/09 -- el usuario tiene que volver a correr "Sincronizar y calcular para mañana" cuando quiera la DT definitiva, ahora sobre la base ya corregida. (2) **Pershing** quedó con saldos negativos aun después de re-extraer el 09/09 (SM001 -105, SM002 -50, KP001 -60) -- vende más de lo que el sistema registra que recibe, o le faltan entradas de días anteriores al 09/09; merece una auditoría propia de ese local. (3) `ventas:sincronizar-diario` cada 30 min es agresivo para la extracción más pesada del sistema -- vigilar si compite por el pool de sesiones de Restaurant con Guías/Kardex; se puede bajar la frecuencia o apagar desde `/admin/sincronizacion`.
- 2026-09-10 · Claude Code · **Optimización de la contención de sesiones de Restaurant tras poner los 6 syncs cada 30 min, pedido explícito del usuario ("optimiza todo")**. Diagnóstico del cuello de botella real: el gateway API-TI (`lib/restaurant-session.js`) mantiene un pool de **solo 4 sesiones** de navegador Playwright (`RESTAURANT_SESSION_POOL_SIZE=4` -- estaba en 8, alguien lo bajó a 4, probablemente por carga de logins/lockout de Restaurant) con un semáforo justo (los que no consiguen slot esperan en cola, no se rechazan). El `worker` general (5 réplicas, `VENTAS_WORKER_REPLICAS=5`) atiende 8 queues y tenía `ventas-pages,ventas-details` **PRIMERAS** en el orden de `--queue` -> Laravel vacía la primera antes de mirar la siguiente, así que una tanda de cientos de jobs de detalle de ventas desplazaba a guías/salidas/requerimientos. `kardex-worker` es contenedor aparte (1 réplica, serial por diseño) pero comparte el mismo pool de 4 del gateway. Medición real: una extracción de Ventas de 3 días = ~24 páginas / ~5 min (corridas nocturnas #59-#64); kardex y guías usan **1 sesión cada una** (serial, el intento de `Http::pool` fue revertido el 2026-09-03); solo Ventas usa las 4. **Fixes aplicados** (commit `5351229` + config de gateway): **(1) Escalonado del ciclo de 30 min** en `routes/console.php` -- cada módulo en su propio carril de 4 min ordenados por criticidad para la DT: `:02` kardex, `:06` guías, `:10` stock-actual, `:14` requerimientos, `:18` salidas, `:22` ventas (antes los 6 en `:00/:30`). Cada módulo tiene el pool casi para sí mismo en su ventana y reusa sesiones tibias del carril anterior. **(2) `compose.yaml`: `--queue` del worker reordenado** -> `ventas-pages,ventas-details` ÚLTIMAS, después de `default` (donde corre `RecalcularSaldoStockJob`, que sí alimenta la DT). Ventas nunca desplaza a nada. **(3) Ventas más liviano en el ciclo de 30 min**: `--dias=0` (solo hoy -- la mitad de páginas, los detalles se saltan solos por `venta_id` ya conocida vía `insertOrIgnore`) + nueva corrida nocturna `--dias=3` a la 01:00 para el barrido profundo con solape. `SincronizarVentasDiario` ahora admite `--dias=0` (antes `max(1, ...)`). **(4) Gateway `/opt/API-TI/.env`: `RESTAURANT_IDLE_SESSION_TTL_MS` 30000 -> 300000** (5 min) -- con carriles de 4 min, una sesión sobrevive de un carril al siguiente en vez de morir a los 30s y re-loguear en frío en cada módulo (pasó de ~24 logins en frío por ciclo de 30 min a ~4). Backup en `/opt/API-TI/.env.bak-20260910`. Gateway recreado (sin extracciones activas al momento), PID 1 = tini, health 200. **NO se tocó `RESTAURANT_SESSION_POOL_SIZE`** -- con el escalonado, 4 sesiones alcanzan (cada módulo salvo ventas usa 1); subirlo solo agregaría carga de login sin beneficio. **Verificado**: `schedule:list` muestra los 6 carriles + ventas nocturna; `docker inspect` confirma el nuevo orden de `--queue`; app/workers/scheduler/gateway healthy, HTTP 200. · **Palancas opcionales NO aplicadas, para si el escalonado no alcanza**: (a) worker dedicado a ventas (contenedor aparte con `--queue=ventas-pages,ventas-details`, como `kardex-worker`) -- aísla del todo; (b) bajar Salidas y Ventas a cada 60 min (son reporting, no alimentan la DT) -- casi sin costo funcional; (c) lock cross-módulo `Cache::lock('gateway-pesado')` que ventas+kardex compartan. Todo se puede prender/apagar por módulo desde `/admin/sincronizacion` sin deploy.
- 2026-09-10 · Claude Code · **`RESTAURANT_SESSION_POOL_SIZE` 4 -> 20, con prueba de carga real en producción, pedido explícito del usuario ("no creo que crashee")**. Config en `/opt/API-TI/.env` (no versionado -- es el env de producción del gateway, igual que `.env.docker` para el CRM). Estado final del `.env` del gateway: `RESTAURANT_SESSION_POOL_SIZE=20`, `RESTAURANT_IDLE_SESSION_TTL_MS=300000`. `VENTAS_WORKER_REPLICAS` quedó en **5** (sin cambio). **Prueba de carga**: se subió temporalmente `VENTAS_WORKER_REPLICAS` a 18 y se dispararon ventas + kardex + guías + stock-actual a la vez para forzar el pool al máximo. **Resultado medido**: el pool abrió las **20/20 sesiones** (~120 procesos chrome, gateway ~6.7 GiB RSS); **RAM del VPS: pico de 10.2 GB de 31 GB (21.9 GB libres), sin swap, sin OOM** -- el VPS NO crashea. **CPU: load average saltó a ~27 en 8 cores durante ~90s** (el "login storm" de 20 `chromium.launch()` + login simultáneos), después bajó solo a ~55% de CPU. `kardex` #67 y `ventas` #65 cerraron `completado` sin un solo error de auth/401/403 -- **Restaurant no se quejó de las 20 sesiones simultáneas**. Se revirtió `VENTAS_WORKER_REPLICAS` a 5 porque **no hace falta**: con el ciclo escalonado, el pico de demanda real de sesiones es ~7 (5 páginas de ventas + 1 kardex + 1 manual), muy por debajo de 20 -- los slots 8-20 son puro colchón para que una extracción manual o el botón de la DT nunca tengan que esperar. Backups del `.env` (`.env.bak-20260910`) borrados tras confirmar estable. · **Advertencia para el futuro**: si alguna vez se sube `VENTAS_WORKER_REPLICAS` para más paralelismo de ventas, el pool ya está en 20 para soportarlo, PERO cada corrida abriría ~N sesiones de golpe y el load average sube fuerte (~27 con 18) por ~1-2 min en cada ciclo de 30 min -- probar con incrementos (8, luego 12) y mirar el load, no saltar a 18+. El número que de verdad controla el paralelismo (y la carga) son las réplicas de workers, no el tamaño del pool.
- 2026-09-10 · Claude Code · El botón "Sincronizar y calcular para mañana" de la Directiva extraía Kardex **solo de HOY** (`fechaInicio = fechaFin = hoy`), confiando en que la sincronización automática ya había cerrado ayer -- justo el supuesto que falló en la auditoría de Aurora. Corregido a **ayer + hoy** (commit `61ad6f9`, desplegado). No hay riesgo de duplicados: `ProcesarLocalKardexJob::reemplazar()` hace `DELETE ... WHERE local_id = ? AND fecha BETWEEN ? AND ?` + `INSERT` dentro de una transacción, así que cada corrida deja exactamente una copia de lo que Restaurant tiene para ese rango, se repita 1 o 100 veces (el reporte de Kardex no trae id de movimiento estable para hacer upsert fila a fila, por eso es wipe+rebuild del rango). Guardas adicionales contra doble-proceso: (1) `$claimed` en `ProcesarLocalKardexJob` -- un job solo procesa un local si su `KardexExtraccionLocal` está `pendiente` o `en_progreso` vencido (>13 min); (2) el botón y `SincronizarKardexDiario` no crean una extracción nueva si ya hay una `pendiente`/`en_progreso` (se enganchan a su id para el poll); (3) el saldo no es incremental -- `StockSaldoRecalculadorService` lo recalcula desde cero (`baseline + ajustes + SUM(entrada) - SUM(salida)` sobre todo Kardex desde `fecha_carga`), así que ni siquiera una fila duplicada podría inflarlo.
- 2026-09-11 · Claude Code · **Nuevo módulo: "Iniciar Directiva de Transferencia"**, pedido explícito del usuario ("debe ser un módulo nuevo no un botón en la vista de directiva-transferencia", pensado para que cualquier persona pueda seguir el flujo). Reemplaza 2 acciones que vivían como modales/botones sueltos en `DirectivaTransferenciaConsolidado` y que además se pisaban en su alcance: el modal "Iniciar Directiva de Transferencia" (alcance + ajustes, pero calculaba YA con los datos que hubiera, sin sincronizar) y el botón "Sincronizar y calcular para mañana" (sincronizaba Kardex+Guías, pero SIEMPRE con el alcance por defecto, ignorando cualquier exclusión/ajuste del otro modal -- nunca se combinaban). Ahora es un flujo único y coherente: se elige el alcance y los ajustes una sola vez, y el mismo botón sincroniza (Kardex ayer+hoy, Guías internas) y calcula usando esa elección. Nuevo `app/Filament/Pages/Stock/IniciarDirectivaTransferencia.php` (slug `stock-inicial/iniciar-directiva`, mismo permiso `directiva-transferencia.view`, sin migración nueva) -- primera página del proyecto que usa formulario nativo de página Filament (`public function form(Schema $schema)` + `$this->form`, patrón ya usado en `ConfigurarIdentidadVisual`/`EditProfile`, solo que ninguna otra pantalla operativa lo había necesitado hasta ahora; todo el resto del proyecto usa formularios dentro de `Action`/modal). Mejoras de interacción reales, no solo mover código: (1) **estado siempre visible arriba** (3 tarjetas: última corrida con total/locales/fecha/hace-cuánto, Kardex sincronizado o no, Guías sincronizadas o no) -- para que nadie tenga que adivinar si conviene sincronizar de nuevo antes de generar; (2) **progreso en vivo** mientras sincroniza (spinner + wire:poll, reemplaza la notificación efímera de antes); (3) **resultado con acción directa** ("Ver la Directiva" -> Consolidado, "Iniciar otra corrida" -> vuelve al formulario) en vez de solo una notificación que se pierde; (4) **historial de las últimas 5 corridas** visible siempre, para confirmar "sí corrió" sin abrir la tabla completa; (5) segundo botón "Calcular sin sincronizar" para cuando los datos ya están frescos y no hace falta esperar (usa el mismo formulario, no repite el criterio por defecto como el viejo "Recalcular"). `DirectivaTransferenciaConsolidado.php` perdió ~230 líneas (las 2 acciones, su estado de poll, sus imports) -- el botón "Iniciar Directiva de Transferencia" del header ahora es un simple link al módulo nuevo; se conservan "Recalcular para mañana" (rápido, sin formulario ni sincronización) y los exports Excel/PDF. **Verificado extremo a extremo en producción, en navegador real** (usuario de diagnóstico temporal, borrado al terminar): el módulo aparece en el nav bajo "Stock Inicial" antes de "Directiva de transferencia"; las 3 tarjetas de estado muestran datos reales (960 sugerencias, Kardex/Guías sincronizados hace 20-22 min); el radio "Locales" es reactivo de verdad (el contador en vivo pasó de "27 de 32" a "32 de 32" al cambiar de "Con venta activa" a "Todos los confirmados", confirmado inspeccionando la respuesta real de Livewire, no solo la pantalla); "Calcular sin sincronizar" generó una corrida real (960 filas, 32 locales, fecha 12/09/2026) y el panel de éxito + la tarjeta de "Última corrida" + el historial se actualizaron solos, sin recargar; "Ver la Directiva" navega al Consolidado con los datos correctos; el link de vuelta desde el Consolidado funciona en el otro sentido. **Deploy sin el script oficial**: `deploy-production.ps1` se negó a correr porque el árbol tenía archivos sin commitear **de otra herramienta** (`VentasExternas*`, con fecha de hoy -- no tocados, no son míos); se desplegó a mano con el mismo mecanismo del script (archive + bundle + rsync + sync de `.git` + build + recreate de `app`/`worker`/`scheduler`/`kardex-worker`), verificado igual de exhaustivo. · **Pendiente/aviso real**: hay trabajo de otra herramienta sin commitear en el working tree local (`app/Models/CanalVentaExterna.php`, `CuotaVentaExterna.php`, `VentaExternaAuditoria.php`, `VentaExternaDiaria.php`, `app/Services/VentasExternasService.php`, 2 migraciones `2026_09_11_09*`) -- no se tocó ni se investigó qué hace, según la regla de convivencia del proyecto; avisar al usuario para que la otra herramienta lo commitee cuando corresponda, antes de que un próximo deploy se vuelva a frenar por esto.
- 2026-09-11 · Claude Code · **Compacta el módulo "Iniciar Directiva de Transferencia", pedido explícito del usuario** ("puede hacerlo una persona de la calle, debe ser lo más práctico y directo, no está compacto"). Sin cambios de lógica/campos, solo de presentación: (1) el estado (última corrida + Kardex + Guías) pasa de 3 tarjetas grandes con texto explicativo a **una sola línea** con puntos de color; (2) se quitan todas las `->description()` de las 2 Section y los helperText largos; labels acortados ("Locales" en vez de "¿A qué locales aplica?", "Ajustes" en vez de "Ajustes opcionales", el radio queda sin label visible con `->hiddenLabel()`); (3) "Corridas recientes" pasa de tabla siempre visible a un `<details>` colapsado. Verificado en producción con navegador real (usuario de diagnóstico, borrado al terminar): todo el módulo entra en una sola pantalla sin scroll (960 sugerencias · 32 locales · fecha (hace X) · Kardex/Guías con punto verde, todo en una línea; radio + Ajustes colapsado + los 2 botones + contador en vivo; Corridas recientes colapsado). · Ninguno.
- 2026-09-11 · Claude Code · **Simplifica más el módulo "Iniciar Directiva de Transferencia" y lo revalida extremo a extremo, pedido explícito del usuario** ("no va" el texto de estado, "no va" el botón "Calcular sin sincronizar" -- el botón que siempre se usa es "Generar DT" que es lo que hacía "Sincronizar y calcular", "no va" el contador de locales, "Corridas recientes" suena feo). Cambios: (1) se quita la línea de estado (última corrida/Kardex/Guías) y el método `estadoActual()` que la alimentaba; (2) se quita el botón "Calcular sin sincronizar" y su método `calcularSinSincronizar()`; (3) el único botón que queda se renombra de "Sincronizar y Calcular" a **"Generar DT"** (método renombrado de `sincronizarYCalcular()` a `generarDt()`); (4) se quita el contador en vivo "X de 32 locales entrarán..." y su método `conteoAlcanceEnVivo()`; (5) "Corridas recientes" -> "Últimas corridas". Sin ningún cambio a la lógica de cálculo (`calcular()` privado intacto) ni a la sincronización (Kardex ayer+hoy + Guías internas, mismo mecanismo de poll). El módulo quedó reducido a: radio "Locales" + "Ajustes" colapsado + un botón + historial colapsado -- nada más. **Bug real de infraestructura encontrado y corregido durante el deploy** (no del código del módulo): tras `docker compose up -d --force-recreate app`, un conflicto de nombre de contenedor (`Error response from daemon: Conflict... container name already in use`) dejó `crm-dimsum-app-1` corriendo con la imagen VIEJA aunque `docker compose build` sí había generado la imagen nueva (confirmado comparando `docker inspect crm-dimsum-app-1 --format '{{.Image}}'` contra `docker inspect crm-dimsum:local --format '{{.Id}}'` -- no coincidían) -- el contenedor se veía "Up X segundos (healthy)" con total normalidad, sin ningún error visible después del conflicto inicial, así que hubiera sido fácil darlo por bueno sin verificar el hash de imagen. Resuelto con `docker rm -f crm-dimsum-app-1` + recrear de nuevo, esta vez limpio. **Lección para futuros deploys de este proyecto**: si `docker compose up -d --force-recreate` muestra un "Conflict" de nombre de contenedor, no asumir que se resolvió solo aunque el contenedor termine "Up (healthy)" -- verificar explícitamente que `.Image` del contenedor coincide con el `.Id` actual de `crm-dimsum:local` antes de dar el deploy por bueno. **Revalidación funcional completa en producción, en navegador real** (usuario de diagnóstico, borrado al terminar): clic en "Generar DT" disparó Kardex #82 (rango ayer 10/09 - hoy 11/09, correcto) y Guías #384; ambos completaron solos; el poll de la pantalla lo detectó sin recargar y calculó automático -- resultado real "810 sugerencias · 27 locales · despacho 12/09/2026" (27/32 = el filtro de venta activa por defecto, consistente con el criterio ya verificado en sesiones anteriores), con el panel de éxito y los botones "Ver Directiva"/"Nueva corrida" renderizados correctamente. **0 bugs funcionales encontrados** en el flujo tras la simplificación. · Ninguno.
- 2026-09-11 · Claude Code · **Analiza y corrige un hallazgo real en producción, pedido explícito del usuario tras ver "Últimas corridas" con una fila rara** ("150 sug. / 5 locales"). **Diagnóstico confirmado contra la base de datos**: esa corrida (11/09 00:56) se generó con el modo "Todos, excepto..." excluyendo los **27 locales activos** en vez de los 5 cerrados -- los 5 locales que sí quedaron procesados son exactamente los 5 ya identificados como cerrados desde el 09/09 (KM 40, Puntamar, Villa María, Primavera, Metro Chorrillos). No es un bug de código -- `calcularParaFecha()` hizo exactamente lo pedido -- es una trampa de interacción real: el multi-select "Excluir" es fácil de usar al revés, y no distingue visualmente locales activos de cerrados. **Consecuencia real y grave, no cosmética**: como `calcularParaFecha()` nunca borra filas de locales que un run no toca, esa corrida dejó **cantidades sugeridas reales y altas para tiendas cerradas** mezcladas en la Directiva de mañana (ej. Metro Chorrillos SIU MAI TRADICIONAL -> 275, Villa María -> 200, Primavera -> 175, KM 40 -> 150) -- con saldo_actual=0 en las 4, la fórmula sacó esos números del promedio histórico de venta de hace 6 días a 4 meses (antes de cerrar), exactamente lo que el filtro "Venta activa (3 días)" existe para evitar. Si alguien hubiera despachado con la Directiva tal cual estaba, se habría mandado mercadería real a locales cerrados. **Corregido**: se borraron las 150 filas de esa corrida (`calculado_en=2026-09-11 00:56:12`) -- la Directiva de mañana quedó limpia en 810 filas / 27 locales, coincidiendo exacto con la corrida correcta de venta activa (11/09 01:26). · **Pendiente/recomendación, no aplicada sin confirmar con el usuario**: el contador en vivo que se quitó el mismo día ("X de 32 locales entrarán en esta corrida") habría hecho evidente el error ANTES de calcular (habría mostrado "5 de 32"); si el usuario lo prefiere, se puede reponer sin todo el texto explicativo de antes, o marcar en el propio Select "Excluir" cuáles locales están activos/cerrados para que el error salte a la vista.
- 2026-09-11 · Claude Code · **Nuevo módulo: "Locales Activos"** (`/admin/stock-inicial/locales-activos`), pedido explícito del usuario tras el incidente de la corrida errónea de esa misma sesión ("no es mejor tener un modulo de locales activos... que sea dinamico en tiempo real con restaurant"). Antes "Venta activa (3 días)" era una heurística invisible (Kardex, 3 días) sin ninguna pantalla donde verla o corregirla -- exactamente lo que permitió que nadie notara el error de la corrida anterior antes de calcular. **Investigado antes de programar** (a pedido explícito de que fuera "dinámico en tiempo real con Restaurant"): Restaurant expone `local_estado`/`local_esventa` por local vía `obtenerLocalesPermitidosParaUsuarioID` -- probado en vivo contra los 37 locales reales, **no sirve**: los 5 locales ya confirmados como cerrados (KM 40, Puntamar, Villa María, Primavera, Metro Chorrillos) siguen marcados `estado=1, es_venta=1` -- es config de catálogo (¿puede vender en el POS?), no indicador de operación real, y nadie la actualiza al cerrar un local (Pershing es la única excepción real, `es_venta=0`, pero SÍ sigue vendiendo según Kardex -- confirma que ese flag tampoco refleja la realidad al revés). La señal dinámica confiable que ya existía es la venta real de Kardex (sincronizado con Restaurant cada 30 min) -- se mantiene como base, pero ahora **visible y corregible**. **Piezas nuevas**: tabla `local_activo_overrides` (local_id, activo, motivo, actualizado_por) + permiso `directiva-transferencia.locales-activos.manage`; `DirectivaTransferenciaService::localesActivos()` (combina automático + override, el override manda siempre) y `ultimaVentaDespachoPorLocal()` (para mostrar "hace cuánto" sin el corte de 3 días); `calcularParaFecha()` ahora llama a `localesActivos()` en vez de `localesConVentaActiva()` directo. Página `LocalesActivos`: tabla de los 32 confirmados con 4 columnas (Automático/Forzado/Usado por la Directiva) + acción modal "Cambiar" (Automático / Forzar activo / Forzar inactivo + motivo). "Iniciar Directiva de Transferencia": "Venta activa (3 días)" -> "Locales activos" (mismo cálculo por dentro) + link "Ver locales activos" junto al botón. **Verificado extremo a extremo en producción, en navegador real** (usuario de diagnóstico, borrado al terminar): la tabla mostró automático correcto para los 32 locales, coincidiendo EXACTO con el estado ya conocido (27 activos "hace 9-11 horas", los 5 cerrados "hace 1 semana" a "hace 4 meses"); se probó un override real (Pershing forzado a inactivo) -- el modal, el guardado, y la columna "Usado por la Directiva" reflejaron el cambio correctamente (Automático=Activo, Forzado=Forzado inactivo, Efectivo=Inactivo), revertido después (`truncate()`); "Generar DT" con "Locales activos" dio **810 sugerencias / 27 locales** -- idéntico al resultado de siempre, confirmando que el nuevo camino de código no cambió el comportamiento por defecto, solo lo hizo visible y corregible. **Deploy**: aplicando la lección de la entrada anterior, se verificó el hash de imagen del contenedor contra `crm-dimsum:local` ANTES de dar el deploy por bueno (coincidieron a la primera esta vez) -- migración corrida automática vía `RUN_MIGRATIONS=true`, tabla y permiso confirmados. · Ninguno.
- 2026-09-11 · Claude Code · **Segunda simplificación de "Iniciar Directiva de Transferencia", pedido explícito del usuario tras ver el módulo "Locales Activos" ya construido**: el radio "Locales activos / Todos / Todos, excepto..." se quita por completo -- desde ahora la corrida siempre calcula sobre `localesActivos()` (`soloVentaActiva` fijo en `true`, sin exclusiones), ya que "Locales Activos" (con su override manual) es la única fuente de verdad sobre qué locales entran, tal como se diseñó esa misma sesión. El toggle "Día sin DT" + sus 2 campos dependientes también se quitan de la sección "Ajustes" -- esa excepción ya se carga en su propio módulo dedicado (`LocalDiaSinDtResource`, existente desde el 09/09), duplicarla acá solo confundía y arriesgaba que alguien cargara la excepción en el lugar equivocado. La sección "Ajustes" queda con un solo campo lógico (el ajuste %) y se le quita el `->columns(2)` que distribuía los 6 campos viejos (2 grupos no relacionados) en un grid roto -- el bug de layout real que el usuario reportó con una captura (toggle/select de un grupo cayendo en la columna del otro). "Últimas corridas" -> "Historial de cálculos" (sonaba informal). **Verificado extremo a extremo en producción, en navegador real** (usuario de diagnóstico, borrado al terminar): el módulo quedó en una sola sección "Ajustes" (toggle "Ajuste %" + 2 campos condicionales, todos en una columna sin cortes) + botón "Generar DT" + link "Ver locales activos" + "Historial de cálculos" colapsado; clic en "Generar DT" disparó Kardex+Guías reales, el poll detectó la finalización sin recargar, y el cálculo con la lógica simplificada (`soloVentaActiva=true` fijo) dio **810 sugerencias · 27 locales · despacho 12/09/2026** -- idéntico al resultado ya conocido de "locales activos", confirmando que quitar el radio no cambió el comportamiento por defecto. **Hallazgo real durante la revalidación** (el usuario pidió "revalida todo lo avanzado"), no corregido en esta sesión, solo documentado: el comando automático nocturno `directiva-transferencia:calcular` (cron diario 03:00, `CalcularDirectivaTransferencia.php`) sigue llamando a `calcularParaFecha($fecha)` **sin** `soloVentaActiva=true` -- a diferencia de "Iniciar Directiva de Transferencia", el cron automático calcula para los 32 locales confirmados, incluidos los 5 ya conocidos como cerrados, sin pasar por el filtro de "Locales Activos" en absoluto. Hoy no representa un riesgo real (las 150 filas que ese cron generó a las 03:00 para los 5 locales cerrados dieron `cantidad_sugerida=0`, porque ya pasaron suficientes semanas sin venta real como para que el promedio de 10 semanas converja a 0), pero es un hueco de diseño real: si un local cerrado tuviera una venta aislada futura (liquidación, error de registro), el cron nocturno la usaría para sugerir despacho sin que nadie la vea ni la pueda forzar a inactivo antes -- exactamente el mismo mecanismo del incidente ya cerrado esta sesión, solo que disparado automáticamente en vez de por un multi-select mal usado. · **Pendiente, a decidir con el usuario**: si conviene que `CalcularDirectivaTransferencia`/el cron de las 03:00 también use `localesActivos()` (mismo criterio que el módulo manual), para cerrar esta inconsistencia de raíz en vez de depender de que el promedio histórico eventualmente llegue a 0 por sí solo.
- 2026-09-11 · Claude Code · **Cierra el hueco de "Locales Activos" en el cron nocturno y en "Recalcular para mañana"**, pedido explícito del usuario tras el hallazgo documentado en la entrada anterior. `CalcularDirectivaTransferencia` (comando `directiva-transferencia:calcular`, cron diario 03:00) y la acción "Recalcular para mañana" de `DirectivaTransferenciaConsolidado` ahora pasan `soloVentaActiva: true` a `calcularParaFecha()`, igual que "Iniciar Directiva de Transferencia" -- los 3 puntos de entrada al cálculo quedan consistentes, con "Locales Activos" (y su override manual) como única fuente de verdad sobre qué locales entran. Se actualizó también el texto del modal de confirmación de "Recalcular" (ya no dice "todos los locales") y la descripción del comando Artisan. **Verificado en producción, no solo en código**: se corrió `php artisan directiva-transferencia:calcular` manualmente contra producción real -- dio **810 sugerencias**, confirmado por consulta directa a la base (`calculado_en` más reciente: 27 locales / 810 filas), coincidiendo exacto con "Locales activos" y ya no con los 32 confirmados de antes. Deploy verificado con hash de imagen + `verify-production.ps1` (522 archivos) antes de la prueba funcional. · Ninguno -- los 3 caminos de cálculo (manual, recalcular rápido, cron automático) quedan alineados.
- 2026-09-11 · Claude Code · **Corrige bug real de raíz reportado por el usuario**: "SIU MAI TRADICIONAL" en DIM SUM PLAZA NORTE mostraba `cantidad_en_transito=0` en la Directiva pese a que el usuario sabía que había una guía real en camino. **Confirmado**: la guía existía en Restaurant (#6017, CD JAP -> Plaza Norte, correlativo 5160, emitida 10/09 16:14, `fecha_traslado` HOY 11/09 12:00, `recepcionada=NO`, 75 unidades de SM001) pero nunca se sincronizó a la copia local (`guias_internas`). **Causa raíz real, con evidencia de código del gateway** (`API-TI/modules/guias-internas/service.js`, función `guideListFilter()`): cuando `GuiasInternasHistoricoService::iniciar()`/`sincronizar()` se llama con `locales=[]` (como hacían tanto "Generar DT" al sincronizar Guías internas, como el cron automático de cada 30 min `guias-internas:sincronizar`), el gateway **NO interpreta eso como "todos los locales"** -- cae al local de la propia sesión de login de Restaurant (`local_id="1"`, que ni siquiera es uno de los 32 locales reales de la cadena), devolviendo solo el pequeño puñado de guías que tocan ese local aislado. Confirmado con datos reales: la corrida automática de ese momento (sync #405) solo trajo **7 cabeceras en 3 días** para toda la empresa -- un número absurdo, coherente con estar limitado a un solo local irrelevante. **Este es el mismo bug ya documentado y corregido en el flujo de Canje Masivo** (bitácora 2026-09-08: "locales vacío NO significa todos para Restaurant"), pero nunca se aplicó ese mismo fix acá -- y afecta la sincronización incremental de Guías internas desde que existe, no solo el caso puntual reportado. La pantalla "Extracción de guías internas" nunca tuvo este bug porque su formulario siempre precarga los locales reales como lista explícita por defecto (`array_column($this->locals, 'id')`), nunca vacío. **Fix**: ambos puntos de entrada afectados (`IniciarDirectivaTransferencia::generarDt()` y `SincronizarGuiasInternas` -- el comando detrás del cron) ahora piden la lista real y vigente de locales a `GuiasInternasGatewayClient::locales()` antes de sincronizar, en vez de pasar `[]`, igual que ya hacía la Extracción manual. **Verificado extremo a extremo en producción, con datos reales**: se corrió `guias-internas:sincronizar` manualmente tras el fix -- pasó de 7 a **86 cabeceras / 976 detalles** en la misma ventana de 3 días, y la guía #6017 (Plaza Norte, SIU MAI TRADICIONAL) apareció en la copia local con sus datos exactos. Recalculando la Directiva después: `cantidad_en_transito` para esa fila pasó de **0 a 75** (las unidades reales en camino) y `cantidad_sugerida` bajó de **250 a 175** en consecuencia -- el sistema ya no ignora mercadería que está genuinamente por llegar. · **Pendiente, no auditado en esta sesión**: no se revisó si el mismo patrón ("`locales=[]` no significa todos") afecta también la sincronización incremental de Requerimientos o Salidas de stock -- el hallazgo se limitó a Guías internas, que es lo que el usuario señaló. Vale la pena una auditoría similar si se sospecha del mismo síntoma (números de sincronización sospechosamente bajos) en esos otros módulos.
- 2026-09-11 · Claude Code · **Auditoría de los otros 4 módulos con sincronización histórica, pedido explícito del usuario tras el fix de Guías internas** ("¿el mismo patrón afecta a los demás?"). Revisado el código real del gateway (`API-TI/modules/*/service.js`) y de cada servicio del CRM que lo llama: **Movimientos entre almacenes** y **Ventas** tienen el MISMO bug latente en el gateway (`locales vacío -> cae al local de la sesión de login`), pero nunca se dispara en la práctica -- Movimientos solo se sincroniza desde la UI manual (`ExtraccionMovimientosAlmacenes`), que siempre precarga la lista completa de locales por defecto; Ventas (`ventas:sincronizar-diario`) siempre pasa una constante hardcodeada con los 38 locales, nunca vacío. **Salidas de stock** y **Requerimientos de stock** tienen el gateway bien diseñado desde el vamos (`locales vacío` sí cae correctamente a "todos" ahí). **Stock Actual** tiene el mismo bug de gateway que Guías internas, pero `StockActualHistoricoService::sincronizar()` **ya tenía el guard correcto** (`if (blank($locales)) $locales = implode('-', $gateway->locals())`) desde antes de esta sesión -- exactamente el mismo patrón de fix que se acaba de aplicar a Guías internas, solo que ahí ya lo habían resuelto. **Conclusión: Guías internas era el único punto realmente afectado** de los 5 módulos -- no se encontró ningún otro caso vivo que corregir. · Ninguno -- auditoría cerrada, sin cambios de código adicionales.
- 2026-09-11 · Claude Code · **Ajustes visuales al PDF de la Directiva de Transferencia, pedido explícito del usuario**: (1) título "Directiva de Transferencia" pasa de estar pegado al logo (alineado a la izquierda) a **centrado** en el ancho de la página; (2) **sombreado interfila** (zebra striping, `tbody tr:nth-child(even)`) para facilitar la lectura de una tabla tan ancha (32 locales); (3) **fecha y hora de generación** (ya existía solo en el pie de página, letra chica, fácil de pasar por alto) ahora también visible junto a "Generado por" en el encabezado, además de mantenerse en el pie de página que se repite en cada hoja. Verificado renderizando el PDF real a imagen (PyMuPDF) antes y después del cambio, contra datos reales de producción -- los 3 ajustes se ven correctos. **Nota de proceso**: durante el despliegue, otra herramienta hizo su propio commit y deploy concurrente sobre este mismo checkout compartido (`c588b2a`, "preservar precisión de costos comerciales", no relacionado) -- `verify-production.ps1` marcó 3 archivos de ESE cambio como no coincidentes (nada que ver con el PDF), verificado aparte que el archivo del PDF sí quedó exactamente como se esperaba dentro del contenedor real. No se tocó nada del cambio de la otra herramienta, según la regla de convivencia del proyecto. · Ninguno.
- 2026-09-11 · Claude Code · **Modal automático de exportar PDF/Excel al terminar "Generar DT"**, pedido explícito del usuario. Extraído el armado del pivote + Excel + PDF (antes solo en `DirectivaTransferenciaConsolidado`, ~150 líneas) a un nuevo servicio compartido `DirectivaTransferenciaExportService` -- el Consolidado ahora solo delega ahí (mismo resultado exacto, sin duplicar lógica), y "Iniciar Directiva de Transferencia" lo reutiliza para sus propios `exportarPdf()`/`exportarExcel()`. Apenas `calcular()` setea `$ultimoResultado`, la página dispara `$this->dispatch('open-modal', id: 'exportar-directiva')`; el modal (`<x-filament::modal>`, primera vez que se usa este componente standalone en el proyecto -- no un Action mounted, ya que la página no tiene tabla ni usa el sistema de Actions) muestra el resumen de la corrida y 2 botones ("Exportar PDF"/"Exportar Excel") con `wire:click` directo a los métodos públicos de la página, cada uno devolviendo un `StreamedResponse` (mismo patrón que ya usan los Action de exportar del Consolidado). **Verificado extremo a extremo en producción, en navegador real**: el modal abre solo apenas termina "Generar DT" (sin recargar, sin ir al Consolidado); invocando `exportarPdf()`/`exportarExcel()` vía el propio Livewire del navegador (interceptando la respuesta real con un hook de `fetch`) se confirmó que Livewire devuelve un efecto `download` real con el PDF embebido en base64 (`contentType: application/pdf`, terminando en el marcador `%%EOF` válido) -- el mecanismo de descarga funciona de punta a punta; la descarga real del archivo no se pudo completar en el navegador de prueba (el sandbox de automatización bloquea cualquier descarga que la propia página dispare), pero eso es una limitación del entorno de prueba, no del código. **Nota de proceso**: el primer intento de probar esto se hizo copiando los 4 archivos directo al contenedor (sin desplegar) para iterar rápido -- pero otra herramienta desplegó su propio commit al mismo tiempo, recreando el contenedor `app` y borrando esos cambios de prueba sin que hubiera ningún error visible; la prueba subsiguiente contra "el modal no aparece" resultó ser una prueba contra código VIEJO, no un bug real. Se resolvió yendo directo al flujo formal (commit + push + `deploy-production.ps1`) antes de volver a probar. **Lección para esta sesión**: cuando hay actividad concurrente de otra herramienta en el mismo checkout compartido, un `docker cp` de prueba sin desplegar puede desaparecer sin aviso -- si algo que se acaba de copiar "no aparece", verificar primero `docker inspect <container> --format '{{.State.StartedAt}}'` contra la hora real antes de asumir que el código tiene un bug. · Ninguno.
- 2026-09-11 · Claude Code · **Verificado extremo a extremo en producción, en navegador real, el nuevo módulo "Tramos de Demanda"** (usuario de diagnóstico, borrado al terminar): selector de local funciona (búsqueda "OVALO" -> "DIM SUM WONG OVALO GUTIERREZ"), las 2 tarjetas de período muestran fechas/horas reales (Tramo 1: 11/09/2026 12:04 -> 12/09/2026 12:00; Tramo 2: 12/09/2026 12:00 -> 14/09/2026 12:00 -- salta domingo por el sábado "sin DT" que tienen los 32 locales confirmados), y la tabla por producto coincide EXACTO con los valores ya verificados a mano por chat con el usuario (ALAS ASADAS: demanda tramo 1 = 4.4, demanda tramo 2 = 47.5). Badges de riesgo de quiebre renderizando bien. Sin bugs encontrados. · Ninguno.
- 2026-09-11 · Claude Code · **Revalidación completa de "Tramos de Demanda", pedido explícito del usuario ("revalida lo implementado")**. Encontrado un hueco real de scoping releyendo el propio código del proyecto: `ScopesLocalsToUser` trae un método `localAllowedForUser()` con un docblock explícito advirtiendo que filtrar las opciones de un Select (`scopeKeyedLocalsToUser()`) **no protege nada por sí solo** -- `localId` es una propiedad pública de Livewire, así que un usuario restringido a ciertos locales podría editar el payload `wire:model` y pedir uno fuera de su alcance. La versión inicial de "Tramos de Demanda" no llamaba a `localAllowedForUser()` en ningún lado. **Corregido**: `ultimoCalculadoEn()`, `periodos()`, la query de la tabla y `updatedLocalId()` ahora validan el local recibido antes de usarlo. **Verificado en vivo, en producción, con un ataque real simulado**: usuario de diagnóstico restringido al local "DIM SUM PLAZA NORTE" (rol Consulta, con el permiso de la Directiva otorgado solo temporalmente para la prueba y revertido después) -- confirmado que el Select solo ofrece su propio local; usando `Livewire.find(...).set('localId', '12')` (Ovalo Gutiérrez, fuera de su alcance) directo desde la consola del navegador, el propio `updatedLocalId()` lo rechazó y reseteó a `null` -- pantalla vacía, cero filtración de datos de otro local. Verificado también, en la revalidación de código (sin cambios necesarios): el cálculo de fechas de tramo1/tramo2 sigue exacto contra los valores ya confirmados con el usuario por chat para Ovalo Gutiérrez y Pershing. · Ninguno -- el módulo queda cerrado sin huecos conocidos.
- 2026-09-11 · Claude Code · **Stock de seguridad estadístico (95% nivel de servicio) en la fórmula de la Directiva de Transferencia**, pedido explícito del usuario tras una charla sobre cómo evitar quiebres de stock en días donde la venta real supera el promedio histórico en 20-30%. Se descartaron 2 alternativas discutidas con el usuario antes de programar: (1) estirar la hora de llegada asumida (mediodía) hasta las 6pm del día siguiente -- mezclaría un dato operativo real con una decisión de negocio, y el usuario terminó prefiriendo la vía estadística; (2) un "+20% fijo" parejo a todos los productos -- ya existe como "Ajuste %" manual, pero infla lo estable igual que lo volátil. **Fórmula elegida**: `stock_seguridad = 1.65 × desviación estándar` (muestral, `n-1`) de las MISMAS hasta-10-semanas ya usadas para el promedio histórico -- 1.65 = nivel de servicio ~95%, elegido explícitamente por el usuario entre 90/95/98% (con ejemplos numéricos concretos mostrados antes de elegir). No hizo falta ninguna data nueva: el array de ventas semanales ya se trae de Kardex para el promedio, la desviación es una cuenta adicional sobre ese mismo array antes de descartarlo. `cantidad_bruta` pasa de `max(0, demanda - proyectado)` a `max(0, (demanda + stock_seguridad) - proyectado)`. Nueva columna `desviacion_estandar` (migración `2026_09_11_170000`, auditable igual que el resto de componentes de la fórmula); "Stock seguridad" se calcula al vuelo en el Consolidado (`DirectivaTransferenciaSugerencia::stockSeguridad()`, `FACTOR_SERVICIO_95=1.65`), ambas columnas ocultas por defecto, mismo patrón que "Demanda tramo 2". **Verificado extremo a extremo en producción, con datos reales**: recálculo real (810 filas, 0 negativas, 0 nulos, 490 filas con variabilidad real medida); comparado antes/después para Ovalo Gutiérrez -- SIU MAI TRADICIONAL (volátil, desviación 37.79→sube de 75 a 150 unidades sugeridas) vs. MIN PAO DULCE (estable, desviación 2.67→colchón de solo 4.4 unidades), confirmando que el colchón se aplica donde de verdad hay riesgo, no parejo. Verificado también en el navegador real: las columnas "Desv. estándar"/"Stock seguridad" aparecen en el selector de columnas del Consolidado y muestran números exactos (`stock_seguridad = 1.65 × desviación`, verificado a mano en 3 filas). · Ninguno -- la fórmula queda verificada sin bugs conocidos, aplicando en las 3 vías de cálculo (manual, recalcular rápido, cron automático) por igual, ya que todas comparten `calcularParaFecha()`.
- 2026-09-11 · Claude Code · **Barrida de huecos funcionales sobre el stock de seguridad, pedido explícito del usuario** ("que huecos funcionales ves, realiza una barrida coherente" -> "realiza de manera ordenada 1 hasta 6"). 5 cambios reales, uno confirmado explícitamente sin tocar: **(1) Doble colchón** (Ajuste % manual aplicado sobre demanda+stock de seguridad, no solo demanda) -- presentada la alternativa de aplicarlo solo sobre demanda pura, el usuario eligió explícitamente dejarlo como estaba; documentado en el código, sin cambio de comportamiento. **(2) Tope al stock de seguridad**: sin límite, una sola semana atípica dentro de las 10 comparadas podía disparar el colchón sin control -- ahora `min(factor × desviación, demanda_promedio)`, verificado en producción con el caso real de mayor desviación (WANTAN en Plaza Norte: colchón sin tope 202.35, con tope igual 202.35 -- no lo tocó; y un segundo caso real donde sí recortó de 6.22 a 5.6). **(3) Nivel de servicio configurable**: antes `FACTOR_SERVICIO_95=1.65` vivía hardcodeado; nueva tabla `directiva_transferencia_settings` (fila única, mismo patrón que `branding_settings`) + página "Stock de seguridad" (`/admin/stock-inicial/stock-seguridad`, permiso `directiva-transferencia.configurar`) con radio 90/95/98%. **(4) Aviso de confianza baja** también en la columna "Stock seguridad" (mismo criterio que "Cantidad sugerida"/"Riesgo de quiebre": <3 semanas de historia). **(5) Visibilidad por defecto**: "Desv. estándar" y "Stock seguridad" pasan de ocultas a visibles por defecto en el Consolidado. **(6) Export Excel/PDF**: antes ninguno de los dos mostraba nada del stock de seguridad ni del riesgo de quiebre -- ahora ambos resaltan en rojo claro las celdas con riesgo (159 celdas reales marcadas en la corrida verificada) con una leyenda al pie, y el Excel suma una hoja "Detalle" (960 filas, una por local×producto) con demanda, desviación, stock de seguridad, saldo, tránsito y ajuste. **3 bugs reales encontrados y corregidos durante la propia verificación en producción, no en teoría**: (a) la primera prueba de `calcularParaFecha()` para validar el cambio repitió el incidente ya documentado el mismo día -- llamarlo sin `soloVentaActiva=true` recalculó los 32 locales confirmados (incluidos los 5 cerrados) en vez de los 27 activos; detectado y corregido de inmediato (960 filas de prueba borradas, recorrida con el criterio correcto -> 810/27, 0 negativas); (b) asignar un float a un campo `decimal:N` de Eloquent (`factor_servicio`) disparaba un deprecation real de `brick/math` -- `DirectivaTransferenciaSetting::NIVELES_DISPONIBLES` pasó a strings; (c) la hoja "Detalle" nueva usaba `setCellValueByColumnAndRow()`, un método que no existe en la versión de PhpSpreadsheet instalada en este proyecto (rompía el export completo) -- corregido a la misma API `setCellValue([col, fila], valor)` que ya usaba el resto del archivo, encontrado recién al invocar el export real contra producción (nunca se manifestó en `php -l` ni en revisión de código). **Nota de deploy**: el script `deploy-production.ps1` volvió a cortar con el falso `NativeCommandError` de PowerShell 5.1 ya documentado (salida informativa de `docker compose`/`git bundle` a stderr) -- esta vez el corte ocurrió ANTES de reconstruir la imagen, dejando el contenedor con código viejo pese a que el repo del servidor ya tenía el commit correcto; detectado comparando el hash de imagen contra `crm-dimsum:local` (no coincidían) y resuelto corriendo manualmente `docker compose -p crm-dimsum --env-file .env.docker build app` + `up -d --force-recreate app worker scheduler kardex-worker` (el `--env-file .env.docker` es necesario -- sin él, `STOCK_GATEWAY_PATH` no se resuelve y el compose falla con "env file /API-TI/.env not found"). **Verificado extremo a extremo en producción, en navegador real** (usuario de diagnóstico, borrado al terminar): cambio de nivel de servicio 95%->90%->95% desde la UI real reflejado en la base de datos (`actualizado_por` registrado), columnas nuevas visibles por defecto con valores y colores correctos en el Consolidado, export Excel (hoja Detalle + celdas de riesgo) y PDF (celdas de riesgo + leyenda) generados sin errores contra datos reales y confirmados visualmente. · Ninguno -- los 6 puntos quedan cerrados y verificados; el estado de producción (810 filas, 27 locales) quedó intacto tras las pruebas.
